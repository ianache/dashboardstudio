---
phase: 21-history-to-graph-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/components/executions/ExecutionHistoryModal.vue, dashboard-app/src/views/IntegrationsView.vue]
autonomous: true
requirements: [FR-02, FR-04, TR-01, TR-02]

must_haves:
  truths:
    - "Clicking the lupa icon in the history table opens the FlowExecutionPopup"
    - "The popup displays the correct flow name and execution ID"
    - "The popup correctly renders the diagram and execution logs for the selected history item"
  artifacts:
    - path: "dashboard-app/src/components/executions/ExecutionHistoryModal.vue"
      provides: "Event emission for graph view"
    - path: "dashboard-app/src/views/IntegrationsView.vue"
      provides: "Event handling and popup orchestration"
  key_links:
    - from: "ExecutionHistoryModal.vue"
      to: "IntegrationsView.vue"
      via: "view-graph event"
    - from: "IntegrationsView.vue"
      to: "FlowExecutionPopup.vue"
      via: "props (executionId, flowId, flowName)"
---

<objective>
Integrate the execution graph visualization trigger into the execution history table.
When a user clicks the "search" (lupa) icon in the history table, the full-screen execution visualizer (FlowExecutionPopup) should open with the context of that specific execution.
</objective>

<execution_context>
@dashboard-app/src/components/executions/ExecutionHistoryModal.vue
@dashboard-app/src/views/IntegrationsView.vue
@dashboard-app/src/components/executions/FlowExecutionPopup.vue
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From dashboard-app/src/components/executions/FlowExecutionPopup.vue:
```javascript
const props = defineProps({
  executionId: { type: String, required: true },
  flowName:    { type: String, default: '' },
  flowId:      { type: String, default: '' }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ExecutionHistoryModal to emit view-graph event</name>
  <files>dashboard-app/src/components/executions/ExecutionHistoryModal.vue</files>
  <action>
    - Update `defineEmits` to include `'view-graph'`.
    - Update `showGraph(ex)` function to emit `'view-graph'` with an object containing `executionId` (from `ex.id`), `flowId` (from `props.flowId`), and `flowName` (from `props.flowName`).
    - Remove the `console.log` or keep it for debugging, but the primary action must be the emission.
  </action>
  <verify>
    <automated>grep "emit('view-graph'" dashboard-app/src/components/executions/ExecutionHistoryModal.vue</automated>
  </verify>
  <done>ExecutionHistoryModal emits view-graph event with correct payload.</done>
</task>

<task type="auto">
  <name>Task 2: Handle view-graph event in IntegrationsView</name>
  <files>dashboard-app/src/views/IntegrationsView.vue</files>
  <action>
    - Clean up redundant `ExecutionHistoryModal` instances in the template (there are two identical ones at the end of the file, keep only one).
    - Add `@view-graph="handleViewGraph"` to the remaining `ExecutionHistoryModal` in the template.
    - Implement `handleViewGraph(event)` in the `<script setup>` block.
    - Inside `handleViewGraph`, set `selectedExecutionId.value = event.executionId`, `selectedFlowId.value = event.flowId`, `selectedFlowName.value = event.flowName`, and set `showExecutionPopup.value = true`.
  </action>
  <verify>
    <automated>grep "handleViewGraph" dashboard-app/src/views/IntegrationsView.vue</automated>
  </verify>
  <done>IntegrationsView correctly handles the event and opens the FlowExecutionPopup with historical data.</done>
</task>

</tasks>

<verification>
Manual verification is required to ensure the UI flow is correct.
1. Open Integrations view.
2. Open History of a flow.
3. Click the lupa icon on a history row.
4. Verify FlowExecutionPopup opens and shows the correct execution details.
</verification>

<success_criteria>
- Event-based communication established between ExecutionHistoryModal and IntegrationsView.
- Historical executions can be visualized in the graph canvas.
- Proper context (ID, flow name) is preserved when opening the visualizer.
</success_criteria>

<output>
After completion, create `.planning/phases/21-history-to-graph-integration/21-01-SUMMARY.md`
</output>
