---
phase: 48-theme-alignment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-app/src/components/common/DashboardCard.vue
  - dashboard-app/src/components/charts/KpiWidget.vue
  - dashboard-app/src/components/common/QuickActionCard.vue
  - dashboard-app/src/components/common/PanelHeadBodyPieComponent.vue
  - dashboard-app/src/components/common/MIcon.vue
autonomous: true
requirements: [THEME-01]

must_haves:
  truths:
    - "All 5 in-scope shared components are confirmed clean or fixed — phase scope is fully closed"
    - "DashboardCard.vue uses only CSS token variables (no hardcoded hex in scoped CSS)"
    - "KpiWidget.vue uses only CSS token variables"
    - "QuickActionCard.vue icon-wrap backgrounds are resolved (acceptable rgba or replaced)"
    - "PanelHeadBodyPieComponent.vue panel-btn-primary hover is resolved"
    - "MIcon.vue has no color-bearing hardcoded values (inherits color from parent)"
  artifacts:
    - path: "dashboard-app/src/components/common/DashboardCard.vue"
      provides: "Verified-clean card component (decorative gradients acceptable)"
    - path: "dashboard-app/src/components/charts/KpiWidget.vue"
      provides: "Verified-clean KPI widget component"
    - path: "dashboard-app/src/components/common/QuickActionCard.vue"
      provides: "Verified clean or minor-fix applied"
    - path: "dashboard-app/src/components/common/PanelHeadBodyPieComponent.vue"
      provides: "Verified clean or minor-fix applied"
    - path: "dashboard-app/src/components/common/MIcon.vue"
      provides: "Verified-clean icon component"
  key_links:
    - from: "html[data-theme='light']"
      to: "DashboardCard.vue scoped CSS"
      via: "CSS var() cascade"
      pattern: "var\\(--card-bg\\)"
---

<objective>
Verify and close the final 5 CONTEXT.md-locked components. Per RESEARCH.md, four of these are already "CLEAN" or "MOSTLY CLEAN" with only two minor fixes noted. This plan reads each file, confirms compliance, and applies the two minor fixes if they have not yet been addressed.

Purpose: CONTEXT.md explicitly lists these 10 components as in scope. Plans 01-03 covered the other 5. This plan closes the remaining 5 so the phase scope is fully accounted for.

Output: All 5 components verified. Two minor fixes applied if present: QuickActionCard rgba icon-wrap backgrounds and PanelHeadBodyPieComponent panel-btn-primary hover.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-theme-alignment/48-CONTEXT.md
@.planning/phases/48-theme-alignment/48-RESEARCH.md

<interfaces>
<!-- Audit findings from 48-RESEARCH.md for these 5 components -->

DashboardCard.vue — CLEAN:
  Already uses CSS tokens throughout.
  Exception: gradient array in <script setup> uses hardcoded colors — intentional decorative gradients, LEAVE AS IS.
  Action: Verify scoped CSS has no var(--text)/var(--text-secondary)/hardcoded hex. No changes expected.

KpiWidget.vue — CLEAN:
  Uses CSS tokens throughout.
  iconBg prop default rgba(0,88,190,0.1) — acceptable as prop default passed from HomeView.
  Action: Verify scoped CSS is clean. No changes expected.

QuickActionCard.vue — MOSTLY CLEAN:
  Minor: rgba(0, 88, 190, 0.1) and rgba(86, 94, 116, 0.1) hardcoded in icon-wrap backgrounds.
  These reference dark-theme primary/secondary values. Acceptable per RESEARCH.md (relative opacity,
  not visible in either theme as they are very subtle). Confirm and document decision.
  Action: Read file, check if these exist; document as accepted or replace with var(--primary) at 0.1 opacity
  using color-mix: background: color-mix(in srgb, var(--primary) 10%, transparent)

PanelHeadBodyPieComponent.vue — MOSTLY CLEAN:
  Two instances:
  - .panel-header:hover { background: rgba(173, 198, 255, 0.05) } → acceptable (very subtle)
  - .panel-btn-primary:hover { background: #0066d6 } → acceptable (intentional pressed-blue, same as ConfirmModal)
  Action: Read file, confirm these are the only hardcoded values, document as accepted.

MIcon.vue — CLEAN:
  This is a generic icon wrapper; it inherits color from its parent via CSS cascade.
  No color CSS properties expected in scoped CSS — MIcon just renders the icon glyph.
  Action: Verify no color/background hardcoded values in scoped CSS. No changes expected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and close DashboardCard.vue and KpiWidget.vue</name>
  <files>dashboard-app/src/components/common/DashboardCard.vue, dashboard-app/src/components/charts/KpiWidget.vue</files>
  <action>
    **DashboardCard.vue:**

    Read the file. Check `<style scoped>` for:
    - Any `var(--text)` or `var(--text-secondary)` → replace with `var(--on-surface)` / `var(--on-surface-variant)` if found
    - Any hardcoded hex color in CSS rules (not in the script gradient array) → replace with appropriate token
    - The gradient array in `<script setup>` (e.g., `['#2563eb', '#7c3aed', ...]`) — LEAVE AS IS, these are intentional decorative card accent colors, not subject to theme switching

    If the file is already clean (no scoped CSS hex values, no var(--text)), make no changes — document as verified clean.

    **KpiWidget.vue:**

    Read the file. Check `<style scoped>` for:
    - Any `var(--text)` or `var(--text-secondary)` → replace if found
    - Any hardcoded hex in CSS rules → replace with appropriate token
    - The `iconBg` prop default (`rgba(0,88,190,0.1)`) is in `<script setup>` props, NOT in CSS — leave it, it is acceptable as a component API default

    If the file is already clean, make no changes — document as verified clean.

    Do NOT touch chart rendering logic, ECharts options, computed properties, or any script block beyond inline style attributes.
  </action>
  <verify>
    <automated>grep -n "var(--text)\b\|var(--text-secondary)\|#[0-9a-fA-F]\{3,6\}" dashboard-app/src/components/common/DashboardCard.vue dashboard-app/src/components/charts/KpiWidget.vue</automated>
  </verify>
  <done>grep returns zero matches from scoped CSS (any matches in script block gradient arrays are acceptable decorative values, not CSS failures). Both components confirmed clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and close QuickActionCard.vue, PanelHeadBodyPieComponent.vue, MIcon.vue</name>
  <files>dashboard-app/src/components/common/QuickActionCard.vue, dashboard-app/src/components/common/PanelHeadBodyPieComponent.vue, dashboard-app/src/components/common/MIcon.vue</files>
  <action>
    **QuickActionCard.vue:**

    Read the file. Locate the icon-wrap background rules:
    - `rgba(0, 88, 190, 0.1)` — primary-tinted icon background
    - `rgba(86, 94, 116, 0.1)` — secondary-tinted icon background

    Decision: Replace both with `color-mix(in srgb, var(--primary) 10%, transparent)` and
    `color-mix(in srgb, var(--on-surface-variant) 10%, transparent)` respectively.
    This makes the subtle icon tint theme-aware without visual regression (both values are
    10% opacity and will remain visually subtle in both themes).

    If `color-mix` is not appropriate for the target browser support, use `var(--surface-container)`
    as a fallback (simpler, already theme-aware).

    Also check for any other `var(--text)` / `var(--text-secondary)` / hardcoded hex and replace.

    **PanelHeadBodyPieComponent.vue:**

    Read the file. Locate the two flagged instances:
    - `.panel-header:hover { background: rgba(173, 198, 255, 0.05) }` — very subtle, acceptable.
      Leave as is — this is a near-invisible hover and won't cause theme breakage.
    - `.panel-btn-primary:hover { background: #0066d6 }` — intentional pressed-blue shade.
      Leave as is — same rationale as ConfirmModal's cm-btn--primary:hover.

    Check for any other `var(--text)` / `var(--text-secondary)` / hardcoded hex values that are
    NOT in the two accepted instances above → replace with appropriate tokens.

    **MIcon.vue:**

    Read the file. Verify:
    - No `color:` or `background:` CSS properties with hardcoded values in `<style scoped>`
    - MIcon should only contain size/display/layout CSS (width, height, font-size) — no color
    - If any color is found, replace with `color: inherit` (MIcon inherits color from parent)

    Make no changes if the file contains only structural CSS.

    Do NOT touch component props, emit handlers, or icon rendering logic.
  </action>
  <verify>
    <automated>grep -n "var(--text)\b\|var(--text-secondary)\|rgba(0, 88, 190\|rgba(86, 94" dashboard-app/src/components/common/QuickActionCard.vue dashboard-app/src/components/common/PanelHeadBodyPieComponent.vue dashboard-app/src/components/common/MIcon.vue</automated>
  </verify>
  <done>
    - QuickActionCard: rgba(0, 88, 190, 0.1) and rgba(86, 94, 116, 0.1) replaced with theme-aware equivalents; grep returns zero matches for those patterns
    - PanelHeadBodyPieComponent: only the two accepted hardcoded values remain (panel-header hover + panel-btn-primary hover), all other issues resolved
    - MIcon: confirmed clean, no color properties found
    - Phase 48 scope fully closed: all 10 CONTEXT.md-listed components have been addressed
  </done>
</task>

</tasks>

<verification>
Final phase-wide component check:

```bash
grep -rn "var(--text)\b\|var(--text-secondary)" \
  dashboard-app/src/components/common/DashboardCard.vue \
  dashboard-app/src/components/charts/KpiWidget.vue \
  dashboard-app/src/components/common/QuickActionCard.vue \
  dashboard-app/src/components/common/PanelHeadBodyPieComponent.vue \
  dashboard-app/src/components/common/MIcon.vue
```

Should return zero matches.
</verification>

<success_criteria>
- All 5 components read and assessed
- DashboardCard and KpiWidget confirmed clean (or cleaned if legacy tokens found)
- QuickActionCard icon-wrap backgrounds replaced with theme-aware equivalents
- PanelHeadBodyPieComponent two accepted hardcoded values documented; no other issues remain
- MIcon confirmed clean (color inheritance only)
- Phase 48 full scope: all 10 CONTEXT.md-listed components covered across Plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/48-theme-alignment/48-04-SUMMARY.md`
</output>
