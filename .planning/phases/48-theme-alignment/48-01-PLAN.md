---
phase: 48-theme-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-app/src/components/layout/SideMenu.vue
  - dashboard-app/src/components/layout/TopBar.vue
  - dashboard-app/src/components/layout/AppLayout.vue
autonomous: true
requirements: [THEME-01]

must_haves:
  truths:
    - "Switching theme toggle changes SideMenu logo background, nav items, and user block instantly"
    - "TopBar background and dropdown panels switch between light and dark surfaces correctly"
    - "Nav-item active/hover states render using token colors in both light and dark themes"
    - "No `var(--text)` or `var(--text-secondary)` remain in shell components (undefined tokens)"
  artifacts:
    - path: "dashboard-app/src/components/layout/SideMenu.vue"
      provides: "Fully token-aware SideMenu — zero hardcoded hex/rgba"
      contains: "var(--primary-container)"
    - path: "dashboard-app/src/components/layout/TopBar.vue"
      provides: "Fully token-aware TopBar — zero legacy tokens, zero hardcoded backgrounds"
      contains: "var(--surface-container-lowest)"
    - path: "dashboard-app/src/components/layout/AppLayout.vue"
      provides: "Verified clean AppLayout"
  key_links:
    - from: "html[data-theme='light']"
      to: "SideMenu.vue scoped CSS"
      via: "CSS var() cascade"
      pattern: "var\\(--primary-container\\)"
    - from: "html[data-theme='light']"
      to: "TopBar.vue scoped CSS"
      via: "CSS var() cascade"
      pattern: "var\\(--surface-container-lowest\\)"
---

<objective>
Migrate all hardcoded colors in the shell layer (SideMenu, TopBar, AppLayout) to CSS custom property tokens so the theme toggle immediately affects every shell element.

Purpose: The shell components have the highest density of hardcoded values (~15 in SideMenu, ~10 in TopBar). Until these are migrated, toggling the theme leaves the most visible UI elements unchanged.

Output: Three shell components using only CSS custom property tokens — no hex values, no rgba with hardcoded dark colors, no undefined legacy tokens (--text, --text-secondary).
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-theme-alignment/48-CONTEXT.md
@.planning/phases/48-theme-alignment/48-RESEARCH.md

<interfaces>
<!-- Token reference — all already defined in main.css -->
<!-- Dark mode: :root { } | Light mode: [data-theme="light"] { } -->

Token → replaces:
  --primary-container      → #2563eb (logo bg, active nav bg, badges)
  --on-primary-container   → #fff on blue backgrounds (logo icon, active nav text)
  --primary                → #3b82f6, #93c5fd (active border-left, active sub-item)
  --surface-container-high → #1e293b, rgba(15,23,42,0.6) (user block bg, nav hover)
  --surface-container      → rgba(0,0,0,0.2) (nav-sub expanded bg)
  --surface-container-lowest → #fff on dropdowns, TopBar bg
  --on-surface             → #fff (workspace-name, hamburger hover, --text)
  --on-surface-variant     → #475569, #64748b, #94a3b8 (subtitle, nav inactive, --text-secondary)
  --outline                → #334155 (workspace avatar border)
  --outline-variant        → dropdown borders
  --error-container        → #fff2f0 danger hover
  --bg                     → page background (AppLayout)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SideMenu.vue — replace all hardcoded colors with CSS tokens</name>
  <files>dashboard-app/src/components/layout/SideMenu.vue</files>
  <action>
    In `<style scoped>`, replace every hardcoded hex/rgba value following the exact mappings from 48-RESEARCH.md:

    CSS rules to update:
    - `.logo-icon-wrap { background: #2563eb }` → `background: var(--primary-container)`
    - `.logo-title { color: #fff }` → `color: var(--on-primary-container)`
    - `.logo-subtitle { color: #475569 }` → `color: var(--on-surface-variant)`
    - `.nav-item { color: #94a3b8 }` → `color: var(--on-surface-variant)`
    - `.nav-item:hover { background: rgba(15,23,42,0.6); color: #f1f5f9 }` → `background: var(--surface-container-high); color: var(--on-surface)`
    - `.nav-item.active { color: #fff; background: rgba(37,99,235,0.12); border-left-color: #3b82f6 }` → `color: var(--on-primary-container); background: var(--primary-container); border-left-color: var(--primary)`
    - `.nav-section-title { color: rgba(255,255,255,0.25) }` → `color: var(--on-surface-variant); opacity: 0.6`
    - `.nav-sub { background: rgba(0,0,0,0.2) }` → `background: var(--surface-container)`
    - `.nav-sub-item { color: rgba(255,255,255,0.4) }` → `color: var(--on-surface-variant)`
    - `.nav-sub-item:hover { color: #fff; background: rgba(255,255,255,0.05) }` → `color: var(--on-surface); background: var(--surface-container-high)`
    - `.nav-sub-item.active { color: #93c5fd }` → `color: var(--primary)`
    - `.workspace-avatar { background: #1e293b; border: 1px solid #334155 }` → `background: var(--surface-container-high); border: 1px solid var(--outline)`
    - `.workspace-name { color: #fff }` → `color: var(--on-surface)`
    - `.workspace-role { color: #475569 }` → `color: var(--on-surface-variant)`
    - `.nav-badge { background: #2563eb; color: #fff }` → `background: var(--primary-container); color: var(--on-primary-container)`
    - Scrollbar thumb `rgba(255,255,255,0.1)` → `var(--outline)`

    In `<template>`, replace inline styles with semantic classes:
    - `<MIcon icon="analytics" ... style="color:#fff" />` → `class="icon-on-primary"` (add to existing classes)
    - `<MIcon icon="person" ... style="color:#64748b" />` → `class="icon-muted"` (add to existing classes)
    - Any other `style="color:#..."` on icons/spans → create corresponding class

    Add to `<style scoped>` at end:
    ```css
    .icon-on-primary { color: var(--on-primary-container); }
    .icon-muted      { color: var(--on-surface-variant); }
    ```

    Do NOT touch theme-toggle buttons (already correct from Phase 47). Do NOT touch component logic.
  </action>
  <verify>
    <automated>grep -n "#[0-9a-fA-F]\{3,6\}\|rgba(15\|rgba(255,255,255\|rgba(37,99\|rgba(0,0,0\|style=\"color" dashboard-app/src/components/layout/SideMenu.vue</automated>
  </verify>
  <done>grep above returns zero matches (no hardcoded colors remain in SideMenu.vue). All nav, logo, user block, and badge elements use CSS token variables.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TopBar.vue — replace hardcoded backgrounds and undefined legacy tokens</name>
  <files>dashboard-app/src/components/layout/TopBar.vue</files>
  <action>
    In `<style scoped>`:

    Replace hardcoded backgrounds:
    - `.top-bar { background: #fff }` (or similar `.top-bar { background: white }`) → `background: var(--surface-container-lowest)`
    - `.dropdown-panel { background: #fff }` → `background: var(--surface-container-lowest); border: 1px solid var(--outline-variant)`
    - `.dp-danger:hover { background: #fff2f0 }` → `background: var(--error-container)`
    - `.hamburger-btn:hover { background: var(--bg) }` → `background: var(--surface-container-high)`
    - `.action-btn:hover { background: var(--bg) }` → `background: var(--surface-container-high)`

    Replace undefined legacy tokens:
    - ALL occurrences of `var(--text)` → `var(--on-surface)`
    - ALL occurrences of `var(--text-secondary)` → `var(--on-surface-variant)`
    - `var(--primary-light)` on `.alert-item.unread` → `var(--surface-container)`
    - `.alert-item.unread:hover { background: #d0e9ff }` → `background: var(--surface-container-high)`

    In `<template>`, replace any inline `style="color:#..."` with scoped classes per Pattern 1 from RESEARCH.md. Add `.icon-muted { color: var(--on-surface-variant); }` and `.icon-primary { color: var(--primary); }` as needed.

    Do NOT touch component logic, event handlers, or non-color CSS properties.
  </action>
  <verify>
    <automated>grep -n "var(--text)\|var(--text-secondary)\|var(--primary-light)\|background: #fff\|background: white\|rgba(15\|style=\"color" dashboard-app/src/components/layout/TopBar.vue</automated>
  </verify>
  <done>grep above returns zero matches. No undefined legacy tokens and no hardcoded background/color values remain in TopBar.vue.</done>
</task>

<task type="auto">
  <name>Task 3: Verify AppLayout.vue is clean</name>
  <files>dashboard-app/src/components/layout/AppLayout.vue</files>
  <action>
    Read AppLayout.vue. According to the research, it already uses `--bg` for backgrounds. Verify this is correct.

    If any hardcoded hex values or undefined legacy tokens (`--text`, `--text-secondary`) exist in `<style scoped>`, apply the same replacements:
    - `--text` → `var(--on-surface)`
    - `--text-secondary` → `var(--on-surface-variant)`
    - Any hardcoded `#hex` → appropriate token from RESEARCH.md token inventory

    If the file is already clean (no hardcoded colors, no undefined tokens), make no changes — just confirm.

    Do NOT touch FlowEditorView, IntegrationsView, or any flow-editor subcomponents even if AppLayout.vue imports them conditionally.
  </action>
  <verify>
    <automated>grep -n "var(--text)\|var(--text-secondary)\|#[0-9a-fA-F]\{3,6\}" dashboard-app/src/components/layout/AppLayout.vue</automated>
  </verify>
  <done>AppLayout.vue either has no changes (already clean) or all remaining issues have been replaced with tokens. grep shows no hardcoded hex colors or undefined legacy tokens.</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the shell layer as a whole:

```bash
grep -rn "var(--text)\|var(--text-secondary)\|var(--primary-light)" dashboard-app/src/components/layout/
grep -rn "background: #fff\|background: white\|rgba(15,23,42\|rgba(255,255,255" dashboard-app/src/components/layout/
```

Both commands should return zero matches in the layout directory.
</verification>

<success_criteria>
- SideMenu.vue: 0 hardcoded hex/rgba values in scoped CSS, 0 inline style="color:#..." in template
- TopBar.vue: 0 occurrences of var(--text), var(--text-secondary), var(--primary-light), 0 hardcoded backgrounds
- AppLayout.vue: clean or already verified clean
- Visually: toggling sun/moon icon transforms SideMenu logo (blue→light-blue), nav items, TopBar surface color
</success_criteria>

<output>
After completion, create `.planning/phases/48-theme-alignment/48-01-SUMMARY.md`
</output>
