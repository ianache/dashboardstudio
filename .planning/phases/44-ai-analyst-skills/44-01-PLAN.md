---
phase: 44-ai-analyst-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-analyst/pyproject.toml
  - ai-analyst/app/core/config.py
  - ai-analyst/app/tools/cube.py
  - ai-analyst/app/main.py
  - ai-analyst/app/agent.py
autonomous: true
requirements: [SVC-02, AGENT-01, AGENT-02]
must_haves:
  truths:
    - "AI Analyst can answer questions about provided screen context"
    - "AI Analyst can generate and execute CubeJS queries using the tool"
    - "CubeJS tool handles JWT authentication correctly"
  artifacts:
    - path: "ai-analyst/app/tools/cube.py"
      provides: "query_data tool function"
    - path: "ai-analyst/app/tools/__init__.py"
      provides: "Module initialization"
  key_links:
    - from: "ai-analyst/app/agent.py"
      to: "ai-analyst/app/tools/cube.py"
      via: "import and tools registration"
    - from: "ai-analyst/app/main.py"
      to: "ChatRequest.screen_context"
      via: "Pydantic model update"
---

<objective>
Implement the CubeJS Query Tool and extend the Chat API to support Screen Context injection.

Purpose: Provide the AI Analyst with data analysis capabilities and visual context awareness.
Output: A functional tool for querying CubeJS and an updated chat endpoint.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ai-analyst-skills/44-RESEARCH.md
@.planning/phases/44-ai-analyst-skills/44-CONTEXT.md
@ai-analyst/app/main.py
@ai-analyst/app/agent.py
@ai-analyst/app/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment & Config Update</name>
  <files>ai-analyst/pyproject.toml, ai-analyst/app/core/config.py</files>
  <action>
    - Add `pyyaml` and `pyjwt` to `ai-analyst/pyproject.toml` dependencies.
    - Run `uv sync` in `ai-analyst/` directory.
    - Update `ai-analyst/app/core/config.py`:
        - Add `cubejs_url: str = "http://cube_api:4000/v1/load"`
        - Add `cubejs_api_secret: str = "welcome1"`
        - Add `skills_catalog_url: str = "https://raw.githubusercontent.com/ianache/skills-catalog/main/catalog.yaml"`
        - Update `gemini_model` default to `gemini-2.5-flash-lite` (or as requested in prompt).
  </action>
  <verify>
    <automated>cd ai-analyst && uv sync && python -c "from app.core.config import get_settings; s = get_settings(); print(s.cubejs_url); assert s.gemini_model == 'gemini-2.5-flash-lite'"</automated>
  </verify>
  <done>Dependencies installed and settings updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CubeJS Query Tool</name>
  <files>ai-analyst/app/tools/cube.py, ai-analyst/app/tools/__init__.py</files>
  <action>
    - Create `ai-analyst/app/tools/__init__.py` (empty).
    - Create `ai-analyst/app/tools/cube.py`:
        - Implement `query_data(query: dict)` async function.
        - Docstring MUST describe it as a tool for fetching business data using CubeJS JSON query format.
        - Include examples in docstring: `{"measures": ["Orders.count"], "dimensions": ["Orders.status"]}`.
        - Use `httpx` to POST to `settings.cubejs_url`.
        - Generate HS256 JWT using `settings.cubejs_api_secret` for the `Authorization` header.
        - Return the `data` array from CubeJS response or informative error.
  </action>
  <verify>
    <automated>python -c "import asyncio; from app.tools.cube import query_data; print(query_data.__doc__); assert 'query_data' in dir()"</automated>
  </verify>
  <done>CubeJS tool implemented with proper docstring and auth.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Screen Context & Register Tool</name>
  <files>ai-analyst/app/main.py, ai-analyst/app/agent.py</files>
  <action>
    - Update `ai-analyst/app/main.py`:
        - Update `ChatRequest` to include `screen_context: dict | None = None`.
        - In `chat` endpoint: if `screen_context` is provided, append a system/context message to the session before calling `runner.run_async`.
        - Pattern: `await session_service.append_message(app_name=APP_NAME, user_id="default", session_id=session_id, role="user", text=f"[CONTEXT] Visible data: {json.dumps(request.screen_context)}")`.
    - Update `ai-analyst/app/agent.py`:
        - Import `query_data` from `app.tools.cube`.
        - Pass `tools=[query_data]` to `LlmAgent` constructor.
        - Update system instruction to acknowledge tools and screen context.
  </action>
  <verify>
    <automated>python -c "from app.agent import root_agent; assert root_agent.tools is not None; print('Tools registered:', [t.__name__ for t in root_agent.tools])"</automated>
  </verify>
  <done>Tool registered and API updated to support screen context.</done>
</task>

</tasks>

<verification>
Check that the agent can be initialized with the new tool and the chat schema accepts screen_context.
</verification>

<success_criteria>
- AI service starts up with `gemini-2.5-flash-lite`.
- `POST /chat` accepts `screen_context`.
- `root_agent` has `query_data` tool registered.
</success_criteria>

<output>
After completion, create `.planning/phases/44-ai-analyst-skills/44-01-SUMMARY.md`
</output>
