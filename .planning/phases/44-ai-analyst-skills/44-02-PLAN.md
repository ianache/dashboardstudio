---
phase: 44-ai-analyst-skills
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - ai-analyst/app/tools/skills.py
  - ai-analyst/app/main.py
  - ai-analyst/app/agent.py
autonomous: true
requirements: [SVC-03, AGENT-03]
must_haves:
  truths:
    - "Skills catalog is fetched from remote URL at startup"
    - "AI Analyst can list and execute skills from the catalog"
  artifacts:
    - path: "ai-analyst/app/tools/skills.py"
      provides: "load_catalog and execute_skill tool"
  key_links:
    - from: "ai-analyst/app/main.py"
      to: "ai-analyst/app/tools/skills.py"
      via: "startup event call to load_catalog"
    - from: "ai-analyst/app/agent.py"
      to: "ai-analyst/app/tools/skills.py"
      via: "execute_skill tool registration"
---

<objective>
Implement the Skills Catalog tool and ensure the catalog is loaded from the remote URL during service startup.

Purpose: Enable the AI Analyst to perform operational tasks defined in an external catalog.
Output: A skills execution tool and a startup lifecycle hook.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ai-analyst-skills/44-RESEARCH.md
@.planning/phases/44-ai-analyst-skills/44-CONTEXT.md
@ai-analyst/app/main.py
@ai-analyst/app/agent.py
@ai-analyst/app/tools/cube.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Skills Tool & Loader</name>
  <files>ai-analyst/app/tools/skills.py</files>
  <action>
    - Create `ai-analyst/app/tools/skills.py`:
        - Define a global `SKILLS_CATALOG: dict = {}`.
        - Implement `async def load_catalog()`:
            - Use `httpx` to GET `settings.skills_catalog_url`.
            - Parse YAML using `yaml.safe_load`.
            - Update `SKILLS_CATALOG` global.
        - Implement `async def execute_skill(skill_id: str, parameters: dict)`:
            - Docstring MUST describe it as a tool for triggering platform skills.
            - Include info about common skills if possible or just general description.
            - Return a confirmation message: `f"Skill {skill_id} triggered with params {parameters}"`.
            - Implementation should check if `skill_id` exists in `SKILLS_CATALOG`.
  </action>
  <verify>
    <automated>python -c "import asyncio; from app.tools.skills import execute_skill; print(execute_skill.__doc__); assert 'execute_skill' in dir()"</automated>
  </verify>
  <done>Skills loader and execution tool implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Lifecycle Integration & Tool Registration</name>
  <files>ai-analyst/app/main.py, ai-analyst/app/agent.py</files>
  <action>
    - Update `ai-analyst/app/main.py`:
        - Add `@app.on_event("startup")` handler.
        - Call `await load_catalog()` from `app.tools.skills`.
    - Update `ai-analyst/app/agent.py`:
        - Import `execute_skill` from `app.tools.skills`.
        - Add `execute_skill` to `root_agent` tools list: `tools=[query_data, execute_skill]`.
        - Update system instruction to mention that skills are available.
  </action>
  <verify>
    <automated>python -c "from app.agent import root_agent; tool_names = [t.__name__ for t in root_agent.tools]; assert 'execute_skill' in tool_names; assert 'query_data' in tool_names; print('Registered tools:', tool_names)"</automated>
  </verify>
  <done>Skills tool registered and startup fetch integrated.</done>
</task>

</tasks>

<verification>
Check that the service starts, fetches the catalog (check logs if possible), and the agent has both tools registered.
</verification>

<success_criteria>
- AI service loads catalog at startup.
- `root_agent` has both `query_data` and `execute_skill` tools.
- System instruction describes capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/44-ai-analyst-skills/44-02-SUMMARY.md`
</output>
