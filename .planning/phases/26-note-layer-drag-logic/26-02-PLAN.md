---
phase: 26-note-layer-drag-logic
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: [dashboard-app/src/views/FlowEditorView.vue, dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-03, FR-08, TR-03]
must_haves:
  truths:
    - "User can drag a Note from the toolbar to the canvas"
    - "Notes are rendered behind connections and functional nodes"
    - "User can move notes by dragging them on the canvas"
    - "Notes persist when saving the diagram"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Notes background layer and drag logic"
  key_links:
    - from: "FlowEditorCanvas.vue"
      to: "FlowEditorView.vue"
      via: "diagramData prop and save event"
---

<objective>
Implement the background layer for notes in the canvas and the logic to drop and move them.

Purpose: Enable users to place and arrange documentation notes on the diagram.
Output: A functional notes layer in the flow editor canvas.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/views/FlowEditorView.vue
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
@dashboard-app/src/stores/integrations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire notes in FlowEditorView</name>
  <files>dashboard-app/src/views/FlowEditorView.vue</files>
  <action>
    Update the `diagramData` computed property to include the `notes` array from `flowStore.currentFlow.flow_notes`.
    Update the `doSave` function to extract `notes` from the canvas data and send it to `flowStore.saveDiagram`.
  </action>
  <verify>
    <automated>grep "flow_notes" dashboard-app/src/views/FlowEditorView.vue</automated>
  </verify>
  <done>FlowEditorView correctly passes and receives the notes array from the canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Note Layer in Canvas</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Add a reactive `notes` ref to `FlowEditorCanvas.vue`.
    - Initialize `notes` from `props.diagramData.notes` in the existing watch for `props.diagramData`.
    - Add `notes` to the snapshot logic and the main state watch.
    - Update `getCurrentDiagramData` to include `notes`.
    - In the `<template>`, add the notes layer inside `.fec-canvas`, BEFORE the SVG layer (`TR-03`).
    - The notes layer should iterate over the `notes` array and render a `div` for each note.
    - Style the note `div` to be absolutely positioned using `node.x` and `node.y`, with a background color from `node.props.color`.
    - For now, display `node.props.content` (pre-markdown).
  </action>
  <verify>
    <automated>grep "notes = ref" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Notes are rendered in the background of the canvas.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Note Drag & Drop and Movement</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Update `onDrop` to detect if the `dragTool.category` is "Annotations". If so, push a new note object to the `notes` array instead of `nodes`.
    - Implement `onNoteMousedown` (similar to `onNodeMousedown`) to set up dragging for a note.
    - Ensure the global mousemove logic in `onGlobalMousemove` handles moving the dragged note.
    - Add a way to select a note (setting `selectedNode`) so its properties appear in the right panel.
    - Ensure deleting the selected node also works if it's a note (check both arrays or use a unified selection state).
  </action>
  <verify>
    <automated>grep "Annotations" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Users can drop new notes onto the canvas and move existing ones.</done>
</task>

</tasks>

<verification>
1. Drag a "Note" from the left panel to the canvas.
2. Verify it appears in the background (behind any existing connections if you move it over them).
3. Move the note to a different position.
4. Click Save.
5. Refresh the page and verify the note is still there in the same position.
</verification>

<success_criteria>
- Notes can be added to the diagram via drag & drop.
- Notes are rendered in the background layer (Z-order).
- Notes can be moved by dragging.
- Notes are persisted in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/26-note-layer-drag-logic/26-02-SUMMARY.md`
</output>
