---
phase: 26-note-layer-drag-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/seed_note_tool.py, dashboard-app/src/stores/toolCatalog.js]
autonomous: true
requirements: [FR-01, FR-02]
must_haves:
  truths:
    - "Note tool is registered in editor_tools table"
    - "Annotations category is defined in CAT_META"
  artifacts:
    - path: "backend/seed_note_tool.py"
      provides: "Tool registration script"
  key_links: []
---

<objective>
Register the "Note" tool in the backend and define the "Annotations" category in the frontend tool catalog.

Purpose: Provide the necessary infrastructure for the "Note" tool to appear in the toolbar.
Output: "Note" tool available in the left panel under a new "Annotations" category.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/stores/toolCatalog.js
@backend/app/models/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed Note Tool (Backend)</name>
  <files>backend/seed_note_tool.py</files>
  <action>
    Create and execute a standalone Python script `backend/seed_note_tool.py` to register the "Note" tool in the `biportal.editor_tools` table.
    
    Tool Details:
    - id: 'tool-note'
    - type: 'note'
    - name: 'Note'
    - subtitle: 'Markdown Sticky Note'
    - icon: 'sticky_note_2'
    - category: 'Annotations'
    - applicable_diagram_types: ['data-integration', 'process-flow', 'data-quality']
    - prop_defs: 
      - content: { label: 'Contenido', type: 'textarea', placeholder: '# Título\nContenido aquí...' }
      - color: { label: 'Color', type: 'select', options: [
          { value: '#fef9c3', label: 'Amarillo' },
          { value: '#dbeafe', label: 'Azul' },
          { value: '#dcfce7', label: 'Verde' },
          { value: '#fce7f3', label: 'Rosa' },
          { value: '#f1f5f9', label: 'Gris' }
        ]}
    - default_props: { content: '# Nueva Nota', color: '#fef9c3' }

    The script should connect to the DB using settings from `.env` (like other scripts in the project).
  </action>
  <verify>
    <automated>python backend/seed_note_tool.py</automated>
  </verify>
  <done>Note tool exists in the database editor_tools table.</done>
</task>

<task type="auto">
  <name>Task 2: Register Annotations Category (Frontend)</name>
  <files>dashboard-app/src/stores/toolCatalog.js</files>
  <action>
    Add the "Annotations" category to the `CAT_META` object in `dashboard-app/src/stores/toolCatalog.js`.
    
    Category Config:
    - label: 'Anotaciones'
    - icon: 'edit_note'
    - color: '#d97706'
    - bg: '#fffbeb'
  </action>
  <verify>
    <automated>grep "Annotations" dashboard-app/src/stores/toolCatalog.js</automated>
  </verify>
  <done>Category "Annotations" is defined in CAT_META.</done>
</task>

</tasks>

<verification>
Start the backend and frontend, go to any flow editor, and verify the "Anotaciones" category appears in the left panel with the "Note" tool inside.
</verification>

<success_criteria>
- The "Note" tool is successfully registered in the database.
- The "Annotations" category is correctly displayed in the frontend toolbar.
</success_criteria>

<output>
After completion, create `.planning/phases/26-note-layer-drag-logic/26-01-SUMMARY.md`
</output>
