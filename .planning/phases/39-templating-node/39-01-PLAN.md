---
phase: 39-templating-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/034_add_nunjucks_template_tool.py
  - backend/app/schemas/schemas.py
  - backend/app/api/endpoints/editor_tools.py
  - dashboard-app/src/components/editor/FlowEditorCanvas.vue
  - backend/app/runtime/runner.ts
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "A Templating node can be added to the flow and configured with a Jinja2/Nunjucks template string."
    - "The property panel for a Templating node includes a live 'Preview' button that renders sample JSON data."
    - "During flow execution, the node renders the template using the input payload and passes the resulting string to the next node."
    - "A malformed template or render error fails the flow with a '[Template Error]' message."
  artifacts:
    - path: "backend/alembic/versions/034_add_nunjucks_template_tool.py"
      provides: "Alembic migration for nunjucks_template tool"
      min_lines: 50
    - path: "backend/app/api/endpoints/editor_tools.py"
      provides: "POST /api/v1/editor-tools/template-preview endpoint"
      min_lines: 20
    - path: "backend/app/schemas/schemas.py"
      provides: "TemplatePreviewRequest schema"
      min_lines: 5
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Preview UI block for Templating node"
      min_lines: 100
    - path: "backend/app/runtime/runner.ts"
      provides: "nunjucks_template execution branch"
      min_lines: 30
  key_links:
    - from: "FlowEditorCanvas.vue (runPreview)"
      to: "backend/app/api/endpoints/editor_tools.py (template_preview)"
      via: "POST /api/v1/editor-tools/template-preview"
      pattern: "axios\\.post\\(['\"]/api/v1/editor-tools/template-preview['\"]"
---

<objective>
Implement the Templating node with a dedicated preview UI and Deno runtime execution.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-templating-node/39-CONTEXT.md
@.planning/phases/39-templating-node/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration — register nunjucks_template tool</name>
  <files>backend/alembic/versions/034_add_nunjucks_template_tool.py</files>
  <action>
Create `backend/alembic/versions/034_add_nunjucks_template_tool.py`.
Chain it from revision `033`.

Tool details:
- `id`: `tool-nunjucks-template-001`
- `type`: `nunjucks_template`
- `name`: `Templating`
- `description`: `Renders a Nunjucks template string against the flow payload`
- `icon`: `description`
- `category`: `transform`
- `prop_defs`:
  ```json
  {
    "template": {
      "label": "Template",
      "type": "code",
      "language": "jinja2",
      "placeholder": "Hello {{ name }}!"
    }
  }
  ```
- `default_props`:
  ```json
  { "template": "Hello {{ name }}!" }
  ```
- `applicable_diagram_types`: `["data-integration", "process-flow"]`

Run migration: `cd backend && uv run alembic upgrade head`
  </action>
  <verify>
    <automated>cd backend && uv run alembic current 2>&1 | grep -E "(034|head)"</automated>
  </verify>
  <done>Alembic reports current revision as `034 (head)`. The `biportal.editor_tools` table contains a row with `type = 'nunjucks_template'`.</done>
</task>

<task type="auto">
  <name>Task 2: Backend — Template Preview Endpoint</name>
  <files>
    - backend/app/schemas/schemas.py
    - backend/app/api/endpoints/editor_tools.py
  </files>
  <action>
1. Add `TemplatePreviewRequest` schema to `backend/app/schemas/schemas.py`:
   ```python
   class TemplatePreviewRequest(BaseModel):
       template: str
       data: Any
   ```

2. Add `POST /template-preview` endpoint to `backend/app/api/endpoints/editor_tools.py`:
   - Use `jinja2.Template` to render the preview.
   - Wrap in try/except to return errors gracefully.
   - Require authentication.
  </action>
  <verify>
    <automated>python -c "from app.api.endpoints.editor_tools import template_preview; print('Import OK')"</automated>
  </verify>
  <done>The `/api/v1/editor-tools/template-preview` endpoint is implemented and accessible.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — Preview UI in FlowEditorCanvas.vue</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
Modify `dashboard-app/src/components/editor/FlowEditorCanvas.vue`:
1. Add `previewDataJson` and `previewResult` reactive variables.
2. Initialize `previewDataJson` with `{}` or sample data when a `nunjucks_template` node is selected.
3. Add a `runPreview` method that calls the backend API using `axios`.
4. Insert the preview UI block in the template, immediately after the property definition loop.
5. Add necessary styles for the preview block and result pre-tag.
  </action>
  <verify>
    <automated>grep "fec-preview-block" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>The property panel shows a Preview section when a Templating node is selected.</done>
</task>

<task type="auto">
  <name>Task 4: Runner — nunjucks_template execution branch</name>
  <files>backend/app/runtime/runner.ts</files>
  <action>
Modify `backend/app/runtime/runner.ts`:
1. Add `import nunjucks from "npm:nunjucks";` at the top.
2. Insert the `else if (node.toolType === 'nunjucks_template')` block in `runNodeExecution()`.
3. Implement rendering logic:
   - Use `nunjucks.renderString(node.props.template, inputPayload)`.
   - Update `context.payload` with the result.
   - Add success and error logging with `[Template Error]` prefix.
  </action>
  <verify>
    <automated>deno check backend/app/runtime/runner.ts</automated>
  </verify>
  <done>The Deno runner correctly handles `nunjucks_template` nodes.</done>
</task>

</tasks>

<verification>
1. **Manual Preview Test**: 
   - Add Templating node.
   - Enter `Hello {{ name }}!` in template.
   - Enter `{"name": "World"}` in Sample Data.
   - Click Preview -> Confirm "Hello World!" appears.
2. **Flow Execution Test**:
   - Source (Data Transform returning `{"name": "Deno"}`) -> Templating -> Destination.
   - Run flow -> Confirm destination receives string "Hello Deno!".
3. **Error Handling Test**:
   - Enter malformed template `{{ name }` -> Confirm Preview shows error and Flow fails with `[Template Error]`.
</verification>
