---
phase: 28-advanced-interaction-persistence
plan: 28-01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "New notes are stored in the 'notes' array instead of 'nodes'"
    - "Notes are rendered behind the SVG connections layer"
    - "Notes can still be dragged, selected, and deleted"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Separate rendering for notes and nodes"
  key_links:
    - from: "onDrop"
      to: "notes.value"
      via: "category check"
---

<objective>
Refactor FlowEditorCanvas to properly separate Notes from functional Nodes in the data structure and rendering layers.

Purpose: Align with the intended architecture where notes live in a background layer and ensure they are saved in the correct DB field (flow_notes).
Output: A cleaner FlowEditorCanvas with dedicated note handling.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate Note Storage logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    Update the `onDrop` function to check the tool category.
    If `dragTool.category` is 'annotations', push the new item to `notes.value` instead of `nodes.value`.
    Ensure the note object has a default width (240) and height (120) in its props if not present.
  </action>
  <verify>
    Add a console.log in onDrop and drag a note; verify it goes to the notes array.
  </verify>
  <done>Notes are correctly routed to the dedicated array on creation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Background Note Layer</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. In the template, locate the `fec-canvas` container.
    2. Add a new loop `v-for="note in notes"` BEFORE the `<svg>` element.
    3. Copy the note-specific rendering logic from the current node loop (the `annotations` check).
    4. Ensure it uses `note` instead of `node`.
    5. Remove the `v-if="node.category?.toLowerCase() === 'annotations'"` block and its associated logic from the main `nodes` loop.
    6. Ensure the note style includes `width` and `height` from `note.props`.
  </action>
  <verify>
    Visually check that notes appear on the canvas and functional nodes (and connections) render on top of them.
  </verify>
  <done>Notes render in a dedicated background layer.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize Interactions for Notes</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. Update `selectNode(item)` to accept either a node or a note.
    2. Update `deleteSelectedNode()` to also filter `notes.value` by `selectedNode.value.id`.
    3. Add `onNoteMousedown(e, note)` to trigger the same dragging logic as `onNodeMousedown` but for notes.
    4. Update `onGlobalMousemove` dragging logic: `if (isDraggingNode && draggedNode)` already works if `draggedNode` is assigned the note object. Verify this.
  </action>
  <verify>
    Drag, select, and delete a note. Verify it works exactly as with normal nodes.
  </verify>
  <done>Notes are fully interactive within their separate storage array.</done>
</task>

</tasks>

<verification>
Check that dropping a note puts it in the `notes` array (inspect via Vue DevTools or temp logging). Verify Z-index (notes behind everything). Verify persistence after Save.
</verification>

<success_criteria>
Notes are successfully isolated from functional nodes while maintaining full interactivity and persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/28-advanced-interaction-persistence/28-01-SUMMARY.md`
</output>
