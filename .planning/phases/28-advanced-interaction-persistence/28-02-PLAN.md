---
phase: 28-advanced-interaction-persistence
plan: 28-02
type: execute
wave: 2
depends_on: [28-01]
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-06, TR-05]
must_haves:
  truths:
    - "Notes have a visible resize handle in the bottom-right corner"
    - "Dragging the handle changes note width and height in real-time"
    - "Notes cannot be resized below 100x100 pixels"
    - "Resized dimensions persist after saving and refreshing"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "NWSE resizing logic"
---

<objective>
Implement dynamic corner-based resizing for Markdown Notes with persistence and boundary constraints.

Purpose: Allow users to customize the size of their annotations to fit different amounts of text.
Output: Resizable notes with 100x100 min-size enforcement.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
@.planning/phases/28-advanced-interaction-persistence/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Resize Handle UI</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. Define `isResizingNote` and `resizingNote` refs in the script setup.
    2. Add a `.fec-note-resizer` element at the end of the note body in the template.
    3. Include a `south_east` Material icon in the resizer.
    4. Attach `@mousedown.stop="onNoteResizeStart($event, note)"` to the resizer.
    5. Add CSS styles for `.fec-note-resizer` (position: absolute, bottom: 0, right: 0, cursor: nwse-resize).
  </action>
  <verify>
    Note on canvas shows the resize handle icon in the corner.
  </verify>
  <done>Resizer UI is present and visually correct.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NWSE Resize Logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. Implement `onNoteResizeStart(e, note)`: set `isResizingNote = true`, `resizingNote = note`.
    2. Update `onGlobalMousemove`:
       - Check `if (isResizingNote.value && resizingNote.value)`.
       - Calculate new width/height based on mouse position relative to note origin.
       - Use `getCanvasPos(e.clientX, e.clientY)` for consistent coordinates.
       - Enforce `Math.max(100, ...)` for both width and height.
    3. Update `onGlobalMouseup`: set `isResizingNote = false`, `resizingNote = null`.
  </action>
  <verify>
    Drag the corner handle and verify the note resizes correctly and stops at 100x100.
  </verify>
  <done>NWSE resizing logic is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Persistence Verification</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. Ensure `getCurrentDiagramData()` and the snapshot logic correctly pick up `note.props.width` and `note.props.height`.
    2. Perform a test: Create note, resize it, save, refresh the page.
    3. Verify the note retains its custom dimensions.
  </action>
  <verify>
    Saved diagram JSON contains 'width' and 'height' in the 'notes' objects.
  </verify>
  <done>Note dimensions are persistent.</done>
</task>

</tasks>

<verification>
Test full resize cycle: Start drag -> Observe real-time update -> Stop drag -> Save -> Reload. Check boundary at 100x100.
</verification>

<success_criteria>
Users can intuitively resize notes, and their choices are saved to the database.
</success_criteria>

<output>
After completion, create `.planning/phases/28-advanced-interaction-persistence/28-02-SUMMARY.md`
</output>
