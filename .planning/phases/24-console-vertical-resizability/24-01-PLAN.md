---
phase: 24-console-vertical-resizability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
autonomous: false
requirements:
  - "FR-02"
  - "FR-03"
  - "FR-04"
  - "TR-02"
  - "TR-03"
  - "UI"
must_haves:
  truths:
    - "User can drag the top edge of the execution console to change its height."
    - "Cursor changes to row-resize when hovering over the resizer handle."
    - "Height limits (100px - 80% viewport) are enforced during dragging."
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Vertical resizability for the console panel"
  key_links:
    - from: "onGlobalMousemove"
      to: "bottomHeight"
      via: "reactive update"
    - from: "bottomHeight"
      to: ".fec-bottom :style"
      via: "inline style binding"
---

<objective>
Implement vertical drag-to-resize functionality for the execution console panel in `FlowEditorCanvas.vue`.

Purpose: Allow users to customize the console height for better visibility of logs.
Output: A resizable bottom panel with min/max height constraints.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
@.planning/phases/22-sidebar-resizability/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reactive state and resize logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    Modify `FlowEditorCanvas.vue` to add vertical resizing logic:
    1. Add `bottomHeight` (default: 240) and `isResizingBottom` (default: false) refs.
    2. Implement `onResizeBottomMousedown(e)` function to set `isResizingBottom.value = true`.
    3. Update `onGlobalMousemove(e)`:
       - Add block to handle `isResizingBottom`.
       - Calculate `newHeight = window.innerHeight - e.clientY`.
       - Apply constraints: `Math.max(100, Math.min(newHeight, window.innerHeight * 0.8))`.
       - Update `bottomHeight.value`.
    4. Update `onGlobalMouseup()`:
       - Ensure `isResizingBottom.value = false` is called.
  </action>
  <verify>
    <automated>grep -q "bottomHeight" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Reactive state and resize handlers are correctly implemented in the script section.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI and Styles</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    Update the template and styles in `FlowEditorCanvas.vue`:
    1. Template:
       - Find `.fec-bottom` element.
       - Add a handle div: `<div class="fec-resizer-v" @mousedown.stop="onResizeBottomMousedown"></div>` inside `.fec-bottom`.
       - Update `:style` binding of `.fec-bottom` to use `bottomHeight + 'px'`.
       - Add `:class="{ 'fec-bottom--resizing': isResizingBottom }"` to `.fec-bottom`.
    2. Styles:
       - Add `.fec-resizer-v` styles: `position: absolute; top: 0; left: 0; right: 0; height: 4px; cursor: row-resize; z-index: 50; transition: background 0.2s;`.
       - Add `.fec-resizer-v:hover` style: `background: rgba(37, 99, 235, 0.2);`.
       - Add `.fec-bottom--resizing` style: `transition: none !important;` (ensure the panel itself doesn't have transition during drag).
  </action>
  <verify>
    <automated>grep -q "fec-resizer-v" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Console panel UI is updated with a resizer handle and dynamic height styles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Console Resizing</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>Human verification of the resizing interaction.</action>
  <verify>Manual check</verify>
  <done>User approved the implementation.</done>
  <what-built>Console vertical resizability</what-built>
  <how-to-verify>
    1. Open the editor and run a flow (or trigger the console visibility).
    2. Hover over the top border of the console.
    3. Verify the cursor changes to `row-resize`.
    4. Click and drag the border up and down.
    5. Verify the console height updates smoothly.
    6. Verify min height (100px) and max height (80% of screen) constraints.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
1. Console remains functional after resizing.
2. No regressions in sidebar resizing (horizontal).
3. No console errors during interaction.
</verification>

<success_criteria>
- Users can vertically resize the execution console.
- Constraints (100px - 80% viewport) are respected.
- Interaction is smooth and intuitive.
</success_criteria>

<output>
After completion, create `.planning/phases/24-console-vertical-resizability/24-01-SUMMARY.md`
</output>
