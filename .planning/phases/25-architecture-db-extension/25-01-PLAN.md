---
phase: 25-architecture-db-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/models/models.py, backend/app/schemas/schemas.py, backend/update_db_phase25.py]
autonomous: true
requirements: [FR-07, TR-04]
---

<objective>
Extend the database schema and Pydantic models to support the new `flow_notes` column in the `IntegrationFlow` table.

Purpose: Provide dedicated persistence for the notes layer in integration flows.
Output: Updated SQLAlchemy models, Pydantic schemas, and a database update script executed.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/models/models.py
@backend/app/schemas/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SQLAlchemy model</name>
  <files>backend/app/models/models.py</files>
  <action>
    Add `flow_notes` column to the `IntegrationFlow` class in `backend/app/models/models.py`.
    - Column type: `JSON`
    - Constraints: `nullable=False`, `default=list`
    - Position: After `flow_metadata`.
  </action>
  <verify>
    <automated>grep "flow_notes = Column(JSON, nullable=False, default=list)" backend/app/models/models.py</automated>
  </verify>
  <done>IntegrationFlow model updated with flow_notes column.</done>
</task>

<task type="auto">
  <name>Task 2: Update Pydantic schemas</name>
  <files>backend/app/schemas/schemas.py</files>
  <action>
    Add `flow_notes` field to the following schemas in `backend/app/schemas/schemas.py`:
    - `IntegrationFlowCreate`: `flow_notes: List[dict] = []`
    - `IntegrationFlowUpdate`: `flow_notes: Optional[List[dict]] = None`
    - `IntegrationFlowResponse`: `flow_notes: List[dict] = []`
  </action>
  <verify>
    <automated>grep "flow_notes" backend/app/schemas/schemas.py</automated>
  </verify>
  <done>Pydantic schemas updated to include flow_notes.</done>
</task>

<task type="auto">
  <name>Task 3: Create and run DB migration script (YOLO)</name>
  <files>backend/update_db_phase25.py</files>
  <action>
    Create a Python script `backend/update_db_phase25.py` to add the `flow_notes` column to the `biportal.integration_flows` table.
    The script should:
    - Use `sqlalchemy.create_engine` with the URL from `get_settings().database_url`.
    - Execute `ALTER TABLE biportal.integration_flows ADD COLUMN IF NOT EXISTS flow_notes JSONB DEFAULT '[]' NOT NULL;`.
    - Note: Using JSONB for performance in PostgreSQL, though SQLAlchemy model uses JSON.
    - Run the script.
  </action>
  <verify>
    <automated>python backend/update_db_phase25.py</automated>
  </verify>
  <done>Database updated with the new column.</done>
</task>

</tasks>

<verification>
Check if backend starts correctly after changes.
</verification>

<success_criteria>
- IntegrationFlow model contains flow_notes.
- IntegrationFlow schemas contain flow_notes.
- IntegrationFlow table in DB contains flow_notes column.
</success_criteria>

<output>
After completion, create `.planning/phases/25-architecture-db-extension/25-01-SUMMARY.md`
</output>
