---
phase: 25-architecture-db-extension
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [backend/app/api/endpoints/integration_flows.py, dashboard-app/src/stores/integrations.js]
autonomous: true
requirements: [FR-07]
---

<objective>
Update the API endpoint to handle the `notes` field and verify the frontend store state.

Purpose: Ensure the separate notes layer is persisted correctly when saving the diagram.
Output: Updated API endpoint and verified frontend store.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/api/endpoints/integration_flows.py
@dashboard-app/src/stores/integrations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update save_flow_diagram endpoint</name>
  <files>backend/app/api/endpoints/integration_flows.py</files>
  <action>
    Modify the `save_flow_diagram` function in `backend/app/api/endpoints/integration_flows.py` to handle the `notes` key in the request body.
    - Check if "notes" is in `body`.
    - If so, assign `body["notes"]` to `flow.flow_notes`.
    - This ensures that when the frontend sends `notes` (the separate layer), it gets persisted in `flow_notes` in the database.
  </action>
  <verify>
    <automated>grep -A 5 "if \"notes\" in body:" backend/app/api/endpoints/integration_flows.py</automated>
  </verify>
  <done>API endpoint handles notes persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Pinia store compatibility</name>
  <files>dashboard-app/src/stores/integrations.js</files>
  <action>
    Review `dashboard-app/src/stores/integrations.js` to ensure that the `currentFlow` state correctly receives and maintains the `flow_notes` field from the API response.
    - The store already assigns the response from `getById` and `update` to `currentFlow`.
    - Ensure that no logic filters out unexpected fields.
    - (Actually, just a confirmation task as the existing code should handle it).
  </action>
  <verify>
    <automated>grep "this.currentFlow = updated" dashboard-app/src/stores/integrations.js</automated>
  </verify>
  <done>Pinia store confirmed to handle flow_notes automatically via response mapping.</done>
</task>

</tasks>

<verification>
Perform a test save from the frontend (in next phase) or use curl to verify the field is saved.
</verification>

<success_criteria>
- PUT /api/v1/integration-flows/{id}/diagram accepts "notes" in body.
- "notes" are stored in "flow_notes" DB column.
- Frontend store reflects flow_notes in state.
</success_criteria>

<output>
After completion, create `.planning/phases/25-architecture-db-extension/25-02-SUMMARY.md`
</output>
