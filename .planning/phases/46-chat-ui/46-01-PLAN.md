---
phase: 46-chat-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/stores/aiAnalyst.js]
autonomous: true
requirements: [CHAT-02, CHAT-03]
must_haves:
  truths:
    - "aiAnalyst store correctly initializes and captures the current dashboard state as screen context."
    - "sendMessage triggers a POST request to /bff/ai/chat and processes the response stream."
  artifacts:
    - path: "dashboard-app/src/stores/aiAnalyst.js"
      provides: "Reactive chat state and SSE streaming logic"
  key_links:
    - from: "dashboard-app/src/stores/aiAnalyst.js"
      to: "/bff/ai/chat"
      via: "fetch with ReadableStream"
---

<objective>
Implement the core logic for the AI Analyst, including state management, dashboard context capture, and real-time response streaming (SSE).

Purpose: Provides the engine that powers the chat interface.
Output: `dashboard-app/src/stores/aiAnalyst.js` Pinia store.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/stores/dashboard.js
@dashboard-app/src/stores/cubejs.js

<interfaces>
From dashboard-app/src/stores/dashboard.js:
```javascript
export const useDashboardStore = defineStore('dashboard', {
  state: () => ({ activeDashboardId: null, dashboards: [] }),
  getters: { activeDashboard: (state) => ... }
})
```

From dashboard-app/src/stores/cubejs.js:
```javascript
export const useCubeStore = defineStore('cubejs', {
  state: () => ({ apiUrl: ..., token: ... }),
  actions: { executeQuery(query) { ... } }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Create aiAnalyst store and context capture</name>
  <files>dashboard-app/src/stores/aiAnalyst.js</files>
  <action>
    Create a new Pinia store `aiAnalyst`.
    1. Define state: `messages` (Array), `loading` (Boolean), `usage` (Object: input_tokens, output_tokens, cost), `isPanelOpen` (Boolean).
    2. Implement `captureScreenContext()`:
       - Access `useDashboardStore().activeDashboard`.
       - Extract dashboard name, description, and list of widgets.
       - For each widget, include its title, type, and `cubeQuery`.
       - Return a JSON object representing the "screen context".
    3. Add `togglePanel()` action to switch `isPanelOpen` state.
  </action>
  <verify>
    <automated>Check if file exists and exports useAiAnalystStore</automated>
  </verify>
  <done>Pinia store initialized with state and context capture logic.</done>
</task>

<task type="auto">
  <name>Task 1.2: Implement sendMessage with SSE streaming</name>
  <files>dashboard-app/src/stores/aiAnalyst.js</files>
  <action>
    Implement the `sendMessage(text)` action in `aiAnalyst.js`:
    1. Capture context using `this.captureScreenContext()`.
    2. Add the user message to the `messages` array.
    3. Add a placeholder 'assistant' message with `streaming: true`.
    4. Use `fetch('/bff/ai/chat', { method: 'POST', body: JSON.stringify({ message: text, context }) })`.
    5. Consume the response using `response.body.getReader()`.
    6. Process chunks:
       - AI service emits JSON events (e.g., `{ "type": "thought", "content": "..." }`, `{ "type": "answer", "content": "..." }`, `{ "type": "usage", "data": { ... } }`).
       - Update the placeholder assistant message reactively.
       - If `type === 'thought'`, append to `thought` field.
       - If `type === 'answer'`, append to `content` field.
       - If `type === 'usage'`, update the store's `usage` state.
       - If `type === 'done'`, set `streaming: false`.
    7. Handle errors by updating the assistant message with an error state.
  </action>
  <verify>
    <automated>Verify sendMessage function presence and logic for handling ReadableStream</automated>
  </verify>
  <done>Streaming chat logic implemented and wired to the store state.</done>
</task>

</tasks>

<verification>
- Store can be imported and initialized.
- `captureScreenContext` returns expected dashboard metadata.
- `sendMessage` correctly handles the fetch request and stream processing (mocking or manual verification).
</verification>

<success_criteria>
- `aiAnalyst` store handles message history and streaming state.
- SSE chunks are correctly parsed and assigned to thought/content fields.
- Usage metrics are updated upon receiving the `usage` event.
</success_criteria>

<output>
After completion, create `.planning/phases/46-chat-ui/46-01-SUMMARY.md`
</output>
