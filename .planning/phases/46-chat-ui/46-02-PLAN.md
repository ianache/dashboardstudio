---
phase: 46-chat-ui
plan: 02
type: execute
wave: 2
depends_on: [46-chat-ui-01]
files_modified: [
  dashboard-app/src/components/dashboard/AiAnalystMessage.vue,
  dashboard-app/src/components/dashboard/AiAnalystPanel.vue,
  dashboard-app/src/views/DashboardDesignerView.vue
]
autonomous: true
requirements: [CHAT-01, CHAT-04, CHAT-05]
must_haves:
  truths:
    - "The AI Analyst panel is visible in the Dashboard Designer and can be toggled."
    - "Messages render correctly with markdown and collapsible sections."
    - "Usage stats are displayed in the panel header or message bubbles."
    - "Skill CTA buttons are functional and trigger store actions."
  artifacts:
    - path: "dashboard-app/src/components/dashboard/AiAnalystMessage.vue"
      provides: "Visual message bubbles with expandable sections"
    - path: "dashboard-app/src/components/dashboard/AiAnalystPanel.vue"
      provides: "Main chat interface"
  key_links:
    - from: "dashboard-app/src/components/dashboard/AiAnalystPanel.vue"
      to: "dashboard-app/src/stores/aiAnalyst.js"
      via: "Pinia store usage"
---

<objective>
Build the visual interface for the AI Analyst, including the chat panel, message bubbles with collapsible sections, and integration into the Dashboard Designer.

Purpose: Provides the user-facing interface for the BI analyst.
Output: `AiAnalystMessage.vue`, `AiAnalystPanel.vue`, and updated `DashboardDesignerView.vue`.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/stores/aiAnalyst.js
@dashboard-app/src/views/DashboardDesignerView.vue

<interfaces>
From dashboard-app/src/stores/aiAnalyst.js (created in Plan 01):
```javascript
export const useAiAnalystStore = defineStore('aiAnalyst', {
  state: () => ({ messages: [], loading: false, usage: { ... }, isPanelOpen: false }),
  actions: { sendMessage(text) { ... }, togglePanel() { ... } }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Create AiAnalystMessage component</name>
  <files>dashboard-app/src/components/dashboard/AiAnalystMessage.vue</files>
  <action>
    Create `AiAnalystMessage.vue`:
    1. Props: `message` (Object).
    2. Sections:
       - **Thought**: Collapsible section (details/summary pattern). Render `message.thought` as plain text or light markdown.
       - **Actions**: Collapsible section. Render list of tools called (if any).
       - **Result**: Main content. Use `marked` + `dompurify` to render `message.content` as HTML.
    3. CTA Buttons: If `message.actions` contains skills with `call_to_action`, render a button that triggers a (stubbed for now or store-based) skill execution action.
    4. Styling: Match Stitch design (Material Symbols, rounded-xl, light borders). Use different background for 'user' vs 'assistant'.
  </action>
  <verify>
    <automated>Check if file exists and contains template with message.content rendering</automated>
  </verify>
  <done>Message bubble component with expandable thought/action sections implemented.</done>
</task>

<task type="auto">
  <name>Task 2.2: Create AiAnalystPanel component</name>
  <files>dashboard-app/src/components/dashboard/AiAnalystPanel.vue</files>
  <action>
    Create `AiAnalystPanel.vue`:
    1. Layout: Fixed-width side panel (e.g., 400px).
    2. Header:
       - Title: "AI Analyst".
       - Usage stats: Display tokens and cost from `aiAnalystStore.usage`.
       - Close button: Calls `aiAnalystStore.togglePanel()`.
    3. Content: Scrollable list of `AiAnalystMessage` components. Auto-scroll to bottom on new messages.
    4. Footer: Chat input with `v-model` and "Send" button. Disable while `aiAnalystStore.loading`.
  </action>
  <verify>
    <automated>Check if file exists and imports AiAnalystMessage</automated>
  </verify>
  <done>Chat panel with message list and input implemented.</done>
</task>

<task type="auto">
  <name>Task 2.3: Integrate into DashboardDesignerView</name>
  <files>dashboard-app/src/views/DashboardDesignerView.vue</files>
  <action>
    Update `DashboardDesignerView.vue`:
    1. Import `AiAnalystPanel` and `useAiAnalystStore`.
    2. Add `AiAnalystPanel` to the template (e.g., at the end of the `designer-view` container).
    3. Use `v-if="aiAnalystStore.isPanelOpen"` or a transition to show/hide the panel.
    4. Ensure the layout adjusts (e.g., dashboard runtime area shrinks or panel overlaps with a shadow).
    5. Wire a toggle button in the toolbar (could be the existing `auto_awesome` button currently labelled "IA Assist").
  </action>
  <verify>
    <automated>Check if DashboardDesignerView.vue imports AiAnalystPanel</automated>
  </verify>
  <done>AI Analyst panel integrated into the dashboard designer UI.</done>
</task>

</tasks>

<verification>
- Panel can be opened via toolbar button.
- Messages appear in the panel when sent.
- Thought and Action sections are expandable.
- Usage stats in the header update as messages are received.
</verification>

<success_criteria>
- Seamless integration of the chat panel in the designer.
- High-fidelity rendering of AI responses with multi-section bubbles.
- Responsive chat input and scrolling message history.
</success_criteria>

<output>
After completion, create `.planning/phases/46-chat-ui/46-02-SUMMARY.md`
</output>
