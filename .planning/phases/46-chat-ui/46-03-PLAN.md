---
phase: 46-chat-ui
plan: 03
type: execute
wave: 1
depends_on: ["46-01", "46-02"]
files_modified:
  - dashboard-app/src/stores/aiAnalyst.js
  - dashboard-app/src/components/dashboard/AiAnalystMessage.vue
  - dashboard-app/src/components/dashboard/AiAnalystPanel.vue
autonomous: true
gap_closure: true
requirements: [CHAT-03, CHAT-04, CHAT-05]

must_haves:
  truths:
    - "Agent responses render three independently collapsible sections: Thought Process, Actions Taken, and Final Result"
    - "Panel header shows input tokens, output tokens, cache hit %, and session cost as separate stats"
    - "Skill CTA buttons appear inside assistant message bubbles when message.actions contains skills, and clicking them triggers executeSkill"
  artifacts:
    - path: "dashboard-app/src/stores/aiAnalyst.js"
      provides: "Store with actions field on assistant messages, cache_hit in usage state, case 'actions' in _processStreamEvent, executeSkill action"
      exports: ["useAiAnalystStore"]
    - path: "dashboard-app/src/components/dashboard/AiAnalystMessage.vue"
      provides: "Message bubble with three collapsible sections (thought, actions, content) and skill CTA buttons"
      min_lines: 60
    - path: "dashboard-app/src/components/dashboard/AiAnalystPanel.vue"
      provides: "Panel header with split input/output tokens and cache_hit % stat"
  key_links:
    - from: "dashboard-app/src/components/dashboard/AiAnalystMessage.vue"
      to: "dashboard-app/src/stores/aiAnalyst.js"
      via: "executeSkill action via emit or store import"
      pattern: "executeSkill"
    - from: "_processStreamEvent"
      to: "msg.actions"
      via: "case 'actions' populates message.actions array"
      pattern: "case 'actions'"
---

<objective>
Close three verification gaps in Phase 46: missing "Actions Taken" collapsible section (CHAT-03), missing cache hit % in usage stats with split token counts (CHAT-04), and completely absent skill CTA buttons (CHAT-05).

Purpose: All five Phase 46 must-haves must pass verification. The streaming core and panel shell are solid; only the message model shape, stream event handling, and component template are incomplete.
Output: Updated store with full message model, updated AiAnalystMessage with three sections + CTA buttons, updated AiAnalystPanel with correct stats display.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-chat-ui/46-02-SUMMARY.md

<interfaces>
<!-- Current store state (aiAnalyst.js lines 5-14) — needs expansion -->
```javascript
state: () => ({
  messages: [],       // assistant messages currently have: { role, content, thought, streaming, error }
  loading: false,
  usage: {
    input_tokens: 0,  // needs: cache_hit field added
    output_tokens: 0,
    cost: 0
  },
  isPanelOpen: false
})
```

<!-- Current _processStreamEvent cases (lines 123-142) — missing 'actions' case -->
```javascript
switch (event.type) {
  case 'thought':   // populates msg.thought
  case 'answer':    // populates msg.content
  case 'usage':     // updates this.usage (no cache_hit handling yet)
  case 'done':      // sets msg.streaming = false
}
```

<!-- AiAnalystMessage template structure — details/summary pattern already established for thought -->
```html
<details class="ai-section ai-section--thought">
  <summary class="ai-section-summary">
    <span class="material-symbols-outlined ai-section-icon">psychology</span>
    <span class="ai-section-label">Razonamiento</span>
    <span class="material-symbols-outlined ai-section-chevron">expand_more</span>
  </summary>
  <div class="ai-section-body ai-thought-body">{{ message.thought }}</div>
</details>
```

<!-- AiAnalystPanel usage stats block (lines 12-19) — needs split tokens + cache_hit -->
```html
<div v-if="store.usage.input_tokens > 0" class="ai-usage-stats">
  <span class="ai-usage-tokens" title="Tokens usados">
    {{ formatTokens(store.usage.input_tokens + store.usage.output_tokens) }}
  </span>
  <span v-if="store.usage.cost > 0" class="ai-usage-cost">
    ${{ store.usage.cost.toFixed(4) }}
  </span>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand store message model and stream event handling</name>
  <files>dashboard-app/src/stores/aiAnalyst.js</files>
  <action>
Make four targeted changes to aiAnalyst.js:

1. **Add `actions` field to assistant placeholder message** (line ~51):
   Change `{ role: 'assistant', content: '', thought: '', streaming: true, error: false }`
   to `{ role: 'assistant', content: '', thought: '', actions: [], streaming: true, error: false }`

2. **Add `cache_hit` to usage state** (lines 8-12):
   Add `cache_hit: 0` to the usage object in state(). Also update the clearMessages action to reset `cache_hit: 0` alongside the other fields.

3. **Add `case 'actions'` to `_processStreamEvent`** after the `case 'thought'` case:
   ```javascript
   case 'actions':
     if (Array.isArray(event.data)) {
       msg.actions = event.data
     } else if (event.content) {
       // Single action object pushed onto array
       msg.actions = [...(msg.actions || []), event.content]
     }
     break
   ```
   The AI service may stream actions as an array in `event.data` or as individual entries in `event.content`. Handle both.

4. **Update `case 'usage'` handler** to include `cache_hit`:
   ```javascript
   case 'usage':
     if (event.data) {
       this.usage = {
         input_tokens: event.data.input_tokens ?? this.usage.input_tokens,
         output_tokens: event.data.output_tokens ?? this.usage.output_tokens,
         cost: event.data.cost ?? this.usage.cost,
         cache_hit: event.data.cache_hit ?? this.usage.cache_hit
       }
     }
     break
   ```

5. **Add `executeSkill` action** at the end of the actions object (before the closing brace):
   ```javascript
   async executeSkill(skillName, params = {}) {
     // Find the last assistant message to append skill result
     const lastAssistantIdx = [...this.messages].reverse().findIndex(m => m.role === 'assistant')
     const msgIndex = lastAssistantIdx >= 0 ? this.messages.length - 1 - lastAssistantIdx : -1

     this.loading = true
     try {
       const response = await fetch('/bff/ai/skill', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         credentials: 'include',
         body: JSON.stringify({ skill: skillName, params })
       })
       const result = await response.json()
       const resultText = result.output || result.result || JSON.stringify(result)
       this.messages.push({
         role: 'assistant',
         content: `**Skill ejecutada: ${skillName}**\n\n${resultText}`,
         thought: '',
         actions: [],
         streaming: false,
         error: !response.ok
       })
     } catch (err) {
       console.error('[aiAnalyst] executeSkill error:', err)
       this.messages.push({
         role: 'assistant',
         content: `Error al ejecutar skill "${skillName}". Intenta nuevamente.`,
         thought: '',
         actions: [],
         streaming: false,
         error: true
       })
     } finally {
       this.loading = false
     }
   }
   ```
  </action>
  <verify>
    <automated>cd dashboard-app && node -e "const fs=require('fs'); const s=fs.readFileSync('src/stores/aiAnalyst.js','utf8'); const checks=['actions: []','cache_hit: 0','case \'actions\'','cache_hit:','executeSkill']; checks.forEach(c=>{ if(!s.includes(c)) throw new Error('Missing: '+c); }); console.log('All checks pass');"</automated>
  </verify>
  <done>Store has: actions field in assistant placeholder, cache_hit in usage state and clearMessages, case 'actions' in _processStreamEvent, cache_hit in usage update, executeSkill action defined.</done>
</task>

<task type="auto">
  <name>Task 2: Add Actions Taken section and skill CTA buttons to AiAnalystMessage</name>
  <files>dashboard-app/src/components/dashboard/AiAnalystMessage.vue</files>
  <action>
Make two additions inside the assistant bubble `<div v-else class="ai-message-bubble ai-message-bubble--assistant">`, placed between the thought `<details>` block and the streaming indicator `<div>`:

**Addition 1 — "Actions Taken" collapsible section** (insert after the thought `</details>`, before the streaming indicator):
```html
<!-- Actions Taken section (collapsible) -->
<details v-if="message.actions && message.actions.length > 0" class="ai-section ai-section--actions">
  <summary class="ai-section-summary ai-section-summary--actions">
    <span class="material-symbols-outlined ai-section-icon">bolt</span>
    <span class="ai-section-label">Acciones realizadas</span>
    <span class="ai-section-badge">{{ message.actions.length }}</span>
    <span class="material-symbols-outlined ai-section-chevron">expand_more</span>
  </summary>
  <div class="ai-section-body ai-actions-body">
    <div v-for="(action, i) in message.actions" :key="i" class="ai-action-item">
      <span class="material-symbols-outlined ai-action-icon">check_circle</span>
      <span class="ai-action-text">{{ typeof action === 'string' ? action : (action.name || action.tool || JSON.stringify(action)) }}</span>
    </div>
  </div>
</details>
```

**Addition 2 — Skill CTA buttons** (insert after the main content `<div>` and before the streaming cursor `<span>`, at the bottom of the assistant bubble):
```html
<!-- Skill CTA buttons -->
<div v-if="message.skills && message.skills.length > 0" class="ai-skill-ctas">
  <button
    v-for="skill in message.skills"
    :key="skill.name"
    class="ai-skill-btn"
    :disabled="store.loading"
    @click="store.executeSkill(skill.name, skill.params || {})"
  >
    <span class="material-symbols-outlined ai-skill-btn-icon">play_arrow</span>
    {{ skill.label || skill.name }}
  </button>
</div>
```

Note: `message.skills` is the list of skills with `call_to_action` flag. These are populated by the agent's response. The store `executeSkill` is called directly from the component — import `useAiAnalystStore` in `<script setup>` and add `const store = useAiAnalystStore()`.

**Script changes:** Add `import { useAiAnalystStore } from '@/stores/aiAnalyst'` and `const store = useAiAnalystStore()` to `<script setup>`.

Also update the assistant placeholder in `sendMessage` (store Task 1 adds `actions: []`) — add corresponding `skills: []` field to the placeholder so the reactive check `message.skills && message.skills.length > 0` works correctly. However, `skills` is a display-only field derived from the agent's `call_to_action` signals. Add a `case 'skills'` in the store's `_processStreamEvent` that populates `msg.skills`:
  - This belongs in Task 1, but add `skills: []` to the assistant placeholder and a `case 'skills': msg.skills = event.data || []; break` handler. Do this here as a follow-up if Task 1 is already committed, otherwise coordinate.

**CSS additions** (in `<style scoped>`):
```css
/* Actions section */
.ai-section--actions {
  background: #f0fdf4;
  border: 1px solid #bbf7d0;
}

.ai-section-summary--actions {
  color: #15803d;
}

.ai-section-badge {
  font-size: 10px;
  font-weight: 700;
  background: #bbf7d0;
  color: #15803d;
  border-radius: 8px;
  padding: 0 5px;
  min-width: 16px;
  text-align: center;
}

.ai-actions-body {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 8px 10px;
  border-top: 1px solid #bbf7d0;
}

.ai-action-item {
  display: flex;
  align-items: flex-start;
  gap: 6px;
  font-size: 12px;
  color: #166534;
  line-height: 1.4;
}

.ai-action-icon {
  font-size: 14px;
  color: #22c55e;
  flex-shrink: 0;
  margin-top: 1px;
}

.ai-action-text { flex: 1; word-break: break-word; }

/* Skill CTA buttons */
.ai-skill-ctas {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 4px;
}

.ai-skill-btn {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 5px 12px;
  border-radius: 16px;
  border: 1px solid #6366f1;
  background: transparent;
  color: #6366f1;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.15s;
  font-family: inherit;
}

.ai-skill-btn:hover:not(:disabled) {
  background: #6366f1;
  color: #fff;
}

.ai-skill-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.ai-skill-btn-icon { font-size: 14px; }
```

Also update the store's `sendMessage` assistant placeholder to include `skills: []` and add `case 'skills': msg.skills = Array.isArray(event.data) ? event.data : []; break` to `_processStreamEvent`. If you already committed Task 1, make these additions now directly in aiAnalyst.js as part of this task.
  </action>
  <verify>
    <automated>cd dashboard-app && node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/dashboard/AiAnalystMessage.vue','utf8'); const checks=['ai-section--actions','Acciones realizadas','ai-skill-ctas','ai-skill-btn','executeSkill','message.actions','message.skills']; checks.forEach(c=>{ if(!s.includes(c)) throw new Error('Missing in AiAnalystMessage.vue: '+c); }); console.log('All checks pass');"</automated>
  </verify>
  <done>AiAnalystMessage.vue has: "Actions Taken" collapsible section using the same details/summary pattern as thought, skill CTA buttons wired to store.executeSkill, store imported in script setup.</done>
</task>

<task type="auto">
  <name>Task 3: Fix AiAnalystPanel usage stats — split tokens and add cache hit %</name>
  <files>dashboard-app/src/components/dashboard/AiAnalystPanel.vue</files>
  <action>
Replace the usage stats block in the panel header (the `<div v-if="store.usage.input_tokens > 0" class="ai-usage-stats">` block, approximately lines 12-19) with:

```html
<!-- Usage stats -->
<div v-if="store.usage.input_tokens > 0 || store.usage.output_tokens > 0" class="ai-usage-stats">
  <span class="ai-usage-tokens ai-usage-tokens--in" title="Tokens de entrada">
    <span class="material-symbols-outlined ai-stat-icon">arrow_downward</span>{{ formatTokens(store.usage.input_tokens) }}
  </span>
  <span class="ai-usage-tokens ai-usage-tokens--out" title="Tokens de salida">
    <span class="material-symbols-outlined ai-stat-icon">arrow_upward</span>{{ formatTokens(store.usage.output_tokens) }}
  </span>
  <span v-if="store.usage.cache_hit > 0" class="ai-usage-cache" title="Cache hit %">
    <span class="material-symbols-outlined ai-stat-icon">cached</span>{{ store.usage.cache_hit.toFixed(0) }}%
  </span>
  <span v-if="store.usage.cost > 0" class="ai-usage-cost">
    ${{ store.usage.cost.toFixed(4) }}
  </span>
</div>
```

Add CSS for the new stat elements in `<style scoped>`. The existing `.ai-usage-tokens` and `.ai-usage-cost` classes stay; add new modifier and helper classes:
```css
.ai-usage-tokens--in { color: #0369a1; background: #e0f2fe; }
.ai-usage-tokens--out { color: #7c3aed; background: #ede9fe; }

.ai-usage-cache {
  display: flex;
  align-items: center;
  gap: 2px;
  font-size: 11px;
  font-weight: 500;
  color: #b45309;
  background: #fef3c7;
  padding: 2px 6px;
  border-radius: 10px;
  font-family: ui-monospace, monospace;
}

.ai-stat-icon {
  font-size: 10px;
  vertical-align: middle;
}
```

Also update the `formatTokens` function to be more compact for the split display (no change in logic needed — existing function already handles k-formatting).

Update the `watch` in `<script setup>` to also watch `store.usage` so the header reactively updates when new usage events arrive. The watch already covers message content changes; add a separate watcher or extend the existing one. Simplest: add `store.usage` to the watch dependencies or rely on Vue reactivity (since `store.usage` is Pinia reactive state, no explicit watch is needed — the template will auto-update).

No script setup changes needed beyond verifying that `store.usage.cache_hit` is accessible (it will be since Task 1 adds it to the Pinia state).
  </action>
  <verify>
    <automated>cd dashboard-app && node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/dashboard/AiAnalystPanel.vue','utf8'); const checks=['cache_hit','ai-usage-cache','ai-usage-tokens--in','ai-usage-tokens--out','arrow_downward','arrow_upward','cached']; checks.forEach(c=>{ if(!s.includes(c)) throw new Error('Missing in AiAnalystPanel.vue: '+c); }); console.log('All checks pass');"</automated>
  </verify>
  <done>AiAnalystPanel header shows three separate stat chips: input tokens (blue), output tokens (purple), cache hit % (amber, only when > 0), and cost (green). Combined token count is gone.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full automated check:

```bash
cd dashboard-app && node -e "
const fs = require('fs');

// Store checks
const store = fs.readFileSync('src/stores/aiAnalyst.js', 'utf8');
['actions: []', 'skills: []', 'cache_hit: 0', \"case 'actions'\", \"case 'skills'\", 'cache_hit:', 'executeSkill'].forEach(c => {
  if (!store.includes(c)) throw new Error('Store missing: ' + c);
});

// Message component checks
const msg = fs.readFileSync('src/components/dashboard/AiAnalystMessage.vue', 'utf8');
['ai-section--actions', 'Acciones realizadas', 'ai-skill-ctas', 'executeSkill', 'message.actions', 'message.skills'].forEach(c => {
  if (!msg.includes(c)) throw new Error('AiAnalystMessage missing: ' + c);
});

// Panel checks
const panel = fs.readFileSync('src/components/dashboard/AiAnalystPanel.vue', 'utf8');
['cache_hit', 'ai-usage-cache', 'ai-usage-tokens--in', 'ai-usage-tokens--out'].forEach(c => {
  if (!panel.includes(c)) throw new Error('AiAnalystPanel missing: ' + c);
});

console.log('All gap closure checks pass');
"
```

Also confirm the build is clean (no import or syntax errors):
```bash
cd dashboard-app && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- CHAT-03: AiAnalystMessage.vue renders three sections — Razonamiento (thought), Acciones realizadas (actions), and main content — each independently collapsible via HTML details/summary.
- CHAT-04: AiAnalystPanel header shows input tokens and output tokens as separate chips (blue / purple), cache hit % chip (amber) when cache_hit > 0, and cost (green).
- CHAT-05: When message.skills contains entries, pill-shaped CTA buttons appear in the message bubble; clicking one calls store.executeSkill(skillName, params) and appends a result message.
- npm run build completes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/46-chat-ui/46-03-SUMMARY.md`
</output>
