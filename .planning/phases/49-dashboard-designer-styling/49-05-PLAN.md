---
phase: 49-dashboard-designer-styling
plan: 05-gap-closure
type: execute
wave: 1
depends_on: ['01', '02', '03']
gap_closure: true
files_modified:
  - dashboard-app/src/assets/main.css
  - dashboard-app/src/views/DashboardDesignerView.vue
autonomous: true
requirements: [THEME-01]
---

<objective>
Align the application's global styling and specific designer view elements with the aesthetic defined in `design/ux-ui/dashboard-ejemplo.html`.
</objective>

<context>
The user indicated that Phase 49 changes need to be closer to the reference HTML.
Key elements from the reference:
1. Sidebar: `#0F172A` background, slate-800 borders, slate-400 text, white hover.
2. Background: `#f7f9fb`.
3. Cards/Surfaces: `bg-white`, border `slate-200`, soft shadow `0px 4px 20px rgba(15,23,42,0.05)`.
4. Typography: Plus Jakarta Sans for headings (h1, h2, h3), Inter for body.
5. Colors: Primary `#0058be`, Primary Container `#2170e4`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Global CSS Variables</name>
  <files>dashboard-app/src/assets/main.css</files>
  <action>
    Update `:root` and `[data-theme="light"]` variables in `main.css` to match the reference:
    - `--background: #f7f9fb;`
    - `--surface: #ffffff;`
    - `--on-surface: #191c1e;`
    - `--primary: #0058be;`
    - `--primary-container: #2170e4;`
    - `--border: #e2e8f0;` (slate-200)
    - `--shadow-md: 0px 4px 20px rgba(15,23,42,0.05);`
    - Set `--font-heading: 'Plus Jakarta Sans', sans-serif;`
    - Set `--font-body: 'Inter', sans-serif;`
  </action>
</task>

<task type="auto">
  <name>Task 2: Refactor Sidebar Component/Styles</name>
  <files>dashboard-app/src/assets/main.css</files>
  <action>
    Locate sidebar styling in `main.css` (or wherever it lives) and apply:
    - Background: `#0F172A`
    - Text: `slate-400`
    - Border-right: `1px solid #1e293b` (slate-800)
    - Hover state: background `#1e293b`, text `#e2e8f0`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Refactor DashboardDesignerView Canvas</name>
  <files>dashboard-app/src/views/DashboardDesignerView.vue</files>
  <action>
    - Ensure the canvas background is `var(--background)`.
    - Apply the dotted grid pattern if missing: `radial-gradient(#0F172A 1px, transparent 1px)` at 0.03 opacity.
    - Standardize widget cards to use `var(--surface)`, `1px solid var(--border)`, and `var(--shadow-md)`.
  </action>
</task>

</tasks>

<success_criteria>
- The UI matches the color palette and sidebar style of the reference HTML.
- Headings use Plus Jakarta Sans.
- Sidebar is dark with high contrast against the light canvas.
</success_criteria>
