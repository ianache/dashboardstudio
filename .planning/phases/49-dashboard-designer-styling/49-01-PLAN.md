---
phase: 49-dashboard-designer-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-app/src/views/DashboardDesignerView.vue
autonomous: true
requirements: [THEME-01]

must_haves:
  truths:
    - "DashboardDesignerView.vue matches the styling standard strictly defined in DESIGN.md without breaking light mode"
    - "Modals render with a clean, theme-aware frosted glass aesthetic"
    - "UI typography is uniformly clean sans-serif"
  artifacts:
    - path: "dashboard-app/src/views/DashboardDesignerView.vue"
      provides: "A fully re-styled designer view matching Phase 48 common components."
  key_links: []

---

<objective>
Refactor `DashboardDesignerView.vue` internal modals, drawers, and specific widget type selectors to fully align with the Modern Corporate design system detailed in `DESIGN.md`, ensuring full light/dark mode compatibility.
</objective>

<context>
During Phase 48, heavy refactoring took place across many views. `DashboardDesignerView.vue` received some CSS variable replacements to support light/dark modes (`var(--text)` to `var(--on-surface)`). However, a deeper inspection reveals legacy elements:
1. `Plus Jakarta Sans` font usage in `font-h1`, `assign-modal-header h2`, and `props-drawer-header h3`.
2. Opaque or solid modal overlays/boxes (`.modal-overlay-assign`, `.assign-modal-box`, `.props-drawer`) missing the correct frosted glass aesthetic (`backdrop-filter`, theme-aware semi-transparent backgrounds and overlays).
3. Minor hardcoded legacy colors or specific hex shades (e.g., `#fff` fallbacks, linear-gradient fixed hexes) that need validation against semantic equivalents.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Refactor Typography to Inter</name>
  <files>dashboard-app/src/views/DashboardDesignerView.vue</files>
  <action>
    Read the file. Find and replace any instance of `font-family: 'Plus Jakarta Sans', system-ui, sans-serif;` (or similar) with `font-family: 'Inter', system-ui, sans-serif; letter-spacing: -0.01em;`. Ensure the generic `.font-h1` class is updated or removed if unused.
  </action>
  <verify>
    <automated>grep -q "font-family: 'Inter'" dashboard-app/src/views/DashboardDesignerView.vue || (echo "Missing Inter font" && exit 1)</automated>
  </verify>
  <done>Inter font applied and Plus Jakarta Sans removed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Modals and Drawers to Theme-Aware Glassmorphism</name>
  <files>dashboard-app/src/views/DashboardDesignerView.vue</files>
  <action>
    Refactor `.modal-overlay-assign`, `.assign-modal-box`, `.props-drawer-overlay`, and `.props-drawer` to support light and dark modes using the existing CSS Custom Property system.
    - Overlays should use CSS variables (e.g., `background-color: color-mix(in srgb, var(--on-surface) 50%, transparent)`) with `backdrop-filter: blur(12px)`.
    - Modal boxes and Drawers should use `background: color-mix(in srgb, var(--surface) 80%, transparent)` with `backdrop-filter: blur(16px)`.
    - Update box shadows and borders to use theme-aware variables (e.g., `border: 1px solid var(--border)` or `border: 1px solid color-mix(in srgb, var(--on-surface) 10%, transparent)`). Do not hardcode rgba values that break light mode.
  </action>
  <verify>
    <automated>grep -q "backdrop-filter: blur" dashboard-app/src/views/DashboardDesignerView.vue || (echo "Missing backdrop-filter" && exit 1)</automated>
  </verify>
  <done>Modals and drawers use theme-aware styling with CSS variables and backdrop-filter.</done>
</task>
</tasks>

<success_criteria>
- `DashboardDesignerView.vue` has no traces of `Plus Jakarta Sans`.
- Modals inside `DashboardDesignerView.vue` mirror the specs defined in `DESIGN.md` and support both light and dark modes.
- No parsing errors in Vue compilation.
</success_criteria>