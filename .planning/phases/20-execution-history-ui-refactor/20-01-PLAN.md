---
phase: 20-execution-history-ui-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/components/executions/ExecutionHistoryModal.vue]
autonomous: true
requirements: [FR-01, FR-03, UI]

must_haves:
  truths:
    - "Each row in the history table has two icons: description (details) and search (graph)"
    - "Clicking the description icon loads the text details as before"
    - "Clicking the search icon triggers a console log with the execution ID"
    - "The buttons follow a consistent icon-button style with hover effects"
  artifacts:
    - path: "dashboard-app/src/components/executions/ExecutionHistoryModal.vue"
      provides: "Updated UI with action icons"
  key_links:
    - from: "ExecutionHistoryModal.vue"
      to: "selectExecution(ex)"
      via: "@click on description icon"
    - from: "ExecutionHistoryModal.vue"
      to: "showGraph(ex)"
      via: "@click on search icon"
---

<objective>
Refactor the execution history table to use icon buttons instead of text-based "Ver" buttons, and add a placeholder for the graphical visualization trigger.

Purpose: Improve UI consistency and prepare for graphical execution history visualization.
Output: Updated `ExecutionHistoryModal.vue` with material icon actions.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/executions/ExecutionHistoryModal.vue
@dashboard-app/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update template and script for icons</name>
  <files>dashboard-app/src/components/executions/ExecutionHistoryModal.vue</files>
  <action>
    - Replace the "Ver" button in the history table with two icon buttons.
    - First button: uses `material-symbols-outlined` with "description" icon, triggers `selectExecution(ex)`.
    - Second button: uses `material-symbols-outlined` with "search" icon, triggers `showGraph(ex)`.
    - Add `showGraph(ex)` method to the script section that logs `console.log("Showing graph for execution:", ex.id)`.
    - Add `title` attributes to buttons for accessibility ("Ver detalles", "Ver gráfico").
  </action>
  <verify>
    - Check if "description" and "search" strings exist in the file.
    - Check if `showGraph` is defined.
  </verify>
  <done>Template updated with two icons per row and placeholder method added.</done>
</task>

<task type="auto">
  <name>Task 2: Style action icons and layout</name>
  <files>dashboard-app/src/components/executions/ExecutionHistoryModal.vue</files>
  <action>
    - Update the CSS to style the new icon buttons.
    - Remove old `.btn-detail` styles.
    - Add `.btn-icon` class:
      - Display: inline-flex, align-center, justify-center.
      - Background: transparent.
      - Padding: 4px.
      - Border-radius: 50% or 4px.
      - Transition: background 0.2s.
      - Color: #64748b (slate-500) or similar.
      - Hover color: #2563eb (blue-600).
      - Hover background: #eff6ff.
    - Update `.actions-cell` (or use a wrapper div in the td) to display icons side-by-side with 8px gap.
  </action>
  <verify>
    - Check for `.btn-icon` definition in `<style scoped>`.
    - Check that `.btn-detail` is gone.
  </verify>
  <done>Icon buttons styled with professional look and hover states.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify UI Refactor</name>
  <what-built>Refactored Execution History Modal with action icons.</what-built>
  <how-to-verify>
    1. Open the Execution History modal for any flow.
    2. Verify that the "Acciones" column now shows two icons (description and search).
    3. Click the "description" icon and ensure it still shows the node logs detail.
    4. Click the "search" (lupa) icon and check the browser console for "Showing graph for execution: ..." message.
    5. Verify the buttons have hover effects.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Material Symbols are rendered correctly (standard "material-symbols-outlined" class used).
- Table layout remains stable with the new icons.
- No regression in "Ver detalles" functionality.
</verification>

<success_criteria>
- `ExecutionHistoryModal.vue` is refactored to use icons.
- "Ver" button is replaced by `description` icon.
- `search` icon is added with placeholder logic.
- UI styling is consistent with the rest of the app.
</success_criteria>

<output>
After completion, create `.planning/phases/20-execution-history-ui-refactor/20-01-SUMMARY.md`
</output>
