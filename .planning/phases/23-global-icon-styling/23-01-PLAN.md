---
phase: 23-global-icon-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/assets/main.css, dashboard-app/src/components/editor/FlowEditorCanvas.vue, dashboard-app/src/components/executions/ExecutionHistoryModal.vue, dashboard-app/src/components/executions/FlowExecutionPopup.vue, dashboard-app/src/views/FlowEditorView.vue]
autonomous: true
requirements: [FR-01, FR-05, TR-01]
must_haves:
  truths:
    - "Icons in ExecutionConsole.vue render as glyphs, not as text like 'delete_sweep'"
    - ".msi class is defined globally in main.css"
    - "Local redundant definitions of .msi are removed from components"
  artifacts:
    - path: "dashboard-app/src/assets/main.css"
      provides: "Global .msi class definition"
  key_links:
    - from: "dashboard-app/src/components/editor/ExecutionConsole.vue"
      to: "dashboard-app/src/assets/main.css"
      via: "Global CSS linkage"
---

<objective>
Move the `.msi` Material Symbols class definition to a global CSS file to ensure consistent icon rendering across all components, specifically fixing the overlapping text issue in the Execution Console.

Purpose: Fix UI regression where icons were showing as text labels because of CSS scoping.
Output: Refactored CSS and working icons in ExecutionConsole.vue.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/assets/main.css
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
@dashboard-app/src/components/editor/ExecutionConsole.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move .msi to main.css</name>
  <files>dashboard-app/src/assets/main.css</files>
  <action>
    Add the `.msi` class definition to `dashboard-app/src/assets/main.css`. 
    Place it after the `.material-symbols-outlined` definition (around line 309).
    
    Use this exact definition:
    ```css
    .msi {
      font-family: 'Material Symbols Outlined';
      font-weight: normal;
      font-style: normal;
      font-size: 20px;
      line-height: 1;
      letter-spacing: normal;
      text-transform: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      white-space: nowrap;
      direction: ltr;
      -webkit-font-smoothing: antialiased;
      flex-shrink: 0;
      user-select: none;
    }
    ```
  </action>
  <verify>
    <automated>grep -Pzo "(?s)\.msi\s*\{.*?\}" dashboard-app/src/assets/main.css</automated>
  </verify>
  <done>.msi class is present in main.css</done>
</task>

<task type="auto">
  <name>Task 2: Remove redundant .msi definitions</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue, dashboard-app/src/components/executions/ExecutionHistoryModal.vue, dashboard-app/src/components/executions/FlowExecutionPopup.vue, dashboard-app/src/views/FlowEditorView.vue</files>
  <action>
    Remove the `.msi { ... }` block from the `<style scoped>` section of the following files:
    - dashboard-app/src/components/editor/FlowEditorCanvas.vue (approx. line 1070)
    - dashboard-app/src/components/executions/ExecutionHistoryModal.vue (approx. line 182)
    - dashboard-app/src/components/executions/FlowExecutionPopup.vue (approx. line 150)
    - dashboard-app/src/views/FlowEditorView.vue (approx. line 205)
    
    Ensure that component-specific overrides (like `.fec-toggle .msi { font-size: 13px; }`) REMAIN in the files. Only remove the base `.msi` class block.
  </action>
  <verify>
    <automated>grep -c "^\.msi {" dashboard-app/src/components/editor/FlowEditorCanvas.vue dashboard-app/src/components/executions/ExecutionHistoryModal.vue dashboard-app/src/components/executions/FlowExecutionPopup.vue dashboard-app/src/views/FlowEditorView.vue</automated>
  </verify>
  <done>Redundant base definitions are removed, leaving only overrides if any.</done>
</task>

<task type="auto">
  <name>Task 3: Verify ExecutionConsole.vue icons</name>
  <files>dashboard-app/src/components/editor/ExecutionConsole.vue</files>
  <action>
    Check `ExecutionConsole.vue` to ensure the buttons in the header use the `.msi` class correctly.
    The current implementation already uses:
    - `<span class="msi" style="font-size:18px">terminal</span>`
    - `<span class="msi" style="font-size:16px">delete_sweep</span>`
    - `<span class="msi" style="font-size:16px">close</span>`
    
    Ensure there are no style overrides within `ExecutionConsole.vue` that might break the icon rendering (like setting a different font-family on .msi).
  </action>
  <verify>
    <automated>grep "class=\"msi\"" dashboard-app/src/components/editor/ExecutionConsole.vue</automated>
  </verify>
  <done>Icons are confirmed to use the .msi class which is now global.</done>
</task>

</tasks>

<verification>
Check that the icons render correctly in the browser. Since I cannot see the UI, I'll trust the global CSS linkage.
</verification>

<success_criteria>
- .msi is defined in main.css.
- No component has a local base .msi definition.
- ExecutionConsole.vue uses the .msi class for its icons.
</success_criteria>

<output>
After completion, create `.planning/phases/23-global-icon-styling/23-01-SUMMARY.md`
</output>
