---
phase: 22-sidebar-resizability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-01, FR-02]
must_haves:
  truths:
    - "Interactive handle is visible on the left edge of the properties panel"
    - "Cursor changes to col-resize when hovering over the handle"
    - "Clicking and dragging the handle updates the sidebar width in real-time"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Sidebar resizing logic"
  key_links:
    - from: ".fec-resizer handle"
      to: "onGlobalMousemove"
      via: "isResizingRight flag"
---

<objective>
Implement the core drag-to-resize mechanism for the right properties sidebar.
Purpose: Allow users to adjust the sidebar width to fit their content.
Output: An interactive resize handle and mouse event logic for width adjustment.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue

<interfaces>
From dashboard-app/src/components/editor/FlowEditorCanvas.vue:
```javascript
const rightCollapsed = ref(false)
function onGlobalMousemove(e) { ... }
function onGlobalMouseup() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add state and resize handle UI</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Add `rightWidth` ref (default 272) and `isResizingRight` ref (default false) in the script section.
    - In the template, add a `<div class="fec-resizer" @mousedown.stop="onResizeMousedown"></div>` at the beginning of the `.fec-right` aside.
    - Add CSS for `.fec-resizer`:
      - position: absolute; left: 0; top: 0; bottom: 0; width: 4px;
      - cursor: col-resize; z-index: 50; transition: background 0.2s;
      - Add hover state: `background: rgba(37,99,235,0.2)` or similar.
    - Implement `onResizeMousedown` to set `isResizingRight.value = true`.
  </action>
  <verify>
    <automated>grep -q "rightWidth" dashboard-app/src/components/editor/FlowEditorCanvas.vue && grep -q "fec-resizer" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>State variables and UI handle are added to the component.</done>
</task>

<task type="auto">
  <name>Task 2: Implement drag event logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Update `onGlobalMousemove`:
      - Add check: `if (isResizingRight.value) { ... }`.
      - Calculate `newWidth = window.innerWidth - e.clientX`.
      - Update `rightWidth.value = newWidth`.
    - Update `onGlobalMouseup`:
      - Set `isResizingRight.value = false`.
    - Update `.fec-right` template to use dynamic width:
      - Add `:style="{ width: rightCollapsed ? '24px' : rightWidth + 'px' }"`
  </action>
  <verify>
    <automated>grep -q "isResizingRight.value" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Dragging the handle updates the sidebar width in real-time.</done>
</task>

</tasks>

<verification>
- Verify that clicking and dragging the left edge of the right sidebar changes its width.
- Verify that the cursor changes to `col-resize` when hovering over the handle.
</verification>

<success_criteria>
- Users can drag the left margin of the properties panel to resize it.
- Sidebar width updates smoothly during drag.
</success_criteria>

<output>
After completion, create `.planning/phases/22-sidebar-resizability/22-01-SUMMARY.md`
</output>
