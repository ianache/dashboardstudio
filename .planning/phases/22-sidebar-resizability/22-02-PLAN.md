---
phase: 22-sidebar-resizability
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-03, FR-04, FR-05]
must_haves:
  truths:
    - "Sidebar cannot be resized smaller than 272px"
    - "Sidebar cannot exceed 50% of the screen width"
    - "Collapsing and expanding the sidebar preserves the user-defined width"
    - "Resizing feels smooth without CSS transition lag"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Sidebar constraints and mode integration"
  key_links:
    - from: "isResizingRight"
      to: "transition: none"
      via: ".fec-right--resizing class"
---

<objective>
Apply size limits, ensure session persistence, and integrate with existing sidebar modes.
Purpose: Prevent UI breakage from extreme resizing and maintain user preferences.
Output: A robust, constrained, and compatible resizable sidebar.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-sidebar-resizability/22-01-SUMMARY.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement size limits and persistence</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Update the width calculation in `onGlobalMousemove` to enforce limits:
      - Min width: 272px.
      - Max width: 50% of `window.innerWidth`.
    - Use `Math.max(272, Math.min(newWidth, window.innerWidth * 0.5))`.
    - Ensure `rightWidth` is initialized correctly (default 272).
  </action>
  <verify>
    <automated>grep -q "Math.max(272" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Sidebar resizing is constrained within safe boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Mode compatibility and visual polish</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Ensure `fec-right--wide` compatibility:
      - When `fec-right--wide` is active (node with code selected), the sidebar should probably expand to at least 500px or the current `rightWidth` if it's larger.
      - Modify the style binding: `:style="{ width: rightCollapsed ? '24px' : (hasWideMode ? Math.max(500, rightWidth) : rightWidth) + 'px' }"`.
    - Hide `.fec-resizer` when `rightCollapsed` is true.
    - Disable CSS `transition: width 0.22s ease` on `.fec-right` while `isResizingRight` is true to avoid "laggy" dragging.
    - Add a class `.fec-right--resizing` to `.fec-right` when `isResizingRight` is true.
    - In CSS, add `.fec-right--resizing { transition: none !important; }`.
  </action>
  <verify>
    <automated>grep -q "fec-right--resizing" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>Resizing works correctly with collapsed/wide modes and feels smooth.</done>
</task>

</tasks>

<verification>
- Test resizing to the minimum limit (272px).
- Test resizing to the maximum limit (50% of screen).
- Test that collapsing and expanding preserves the custom width.
- Test that selecting a node with code (Wide Mode) works as expected.
</verification>

<success_criteria>
- Sidebar width is constrained between 272px and 50% of screen.
- Resizing is smooth (no transition lag).
- Wide mode and Collapsed mode interact predictably with user-defined width.
</success_criteria>

<output>
After completion, create `.planning/phases/22-sidebar-resizability/22-02-SUMMARY.md`
</output>
