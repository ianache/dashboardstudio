---
phase: 27-markdown-engine-styling
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-05, UI]
must_haves:
  truths:
    - "Selecting a note shows a styling header above it"
    - "Toolbar includes 5 color buttons"
    - "Clicking a color button changes the note background instantly"
    - "Toolbar includes font size + and - buttons"
    - "Font size buttons increase/decrease the text size within reasonable limits"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Floating styling toolbar for notes"
---

<objective>
Implement the on-canvas styling toolbar for notes, allowing users to change colors and font sizes without using the sidebar.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement styling header UI</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Add a `.fec-note-header` div inside the `.fec-note` container.
    - Use `v-if="selectedNote?.id === note.id || editingNoteId === note.id"` to control visibility.
    - Inside the header, add:
      - A color palette container with 5 color circles (Yellow, Blue, Green, Pink, Grey).
      - Font size buttons with icons `format_size` or simply `+` and `-` (using `msi` icons like `add` and `remove`).
    - Apply CSS to position the header absolutely above the note (e.g., `top: -40px`) and style it as a floating bar.
  </action>
  <verify>
    - Selecting a note makes the header appear.
    - Header contains the requested controls.
    - Header is positioned correctly even when zooming.
  </verify>
  <done>Styling header UI is implemented and visible when appropriate.</done>
</task>

<task type="auto">
  <name>Task 2: Implement styling logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Implement `changeNoteColor(note, color)` method.
      - Palette colors: Yellow (#fef9c3), Blue (#dbeafe), Green (#dcfce7), Pink (#fce7f3), Grey (#f1f5f9).
    - Implement `changeNoteFontSize(note, delta)` method.
      - Delta +2 or -2.
      - Keep font size between 8px and 48px.
    - Update the color circles and font buttons to call these methods.
    - Ensure note background and content styles react to these property changes.
  </action>
  <verify>
    - Clicking color buttons changes note background.
    - Clicking +/- buttons changes font size of the note content.
  </verify>
  <done>Note styling properties can be updated via the canvas toolbar.</done>
</task>

</tasks>

<verification>
- Select a note.
- Click the Blue palette option. Note should turn blue.
- Click the + button 3 times. Text should grow larger.
- Click the - button. Text should grow smaller.
</verification>

<success_criteria>
- FR-05 is fully implemented.
- UI requirement for header visibility is met.
</success_criteria>

<output>
After completion, create `.planning/phases/27-markdown-engine-styling/27-02-SUMMARY.md`
</output>
