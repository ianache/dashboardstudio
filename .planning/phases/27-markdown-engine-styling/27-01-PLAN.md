---
phase: 27-markdown-engine-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard-app/src/components/editor/FlowEditorCanvas.vue]
autonomous: true
requirements: [FR-04, TR-01, TR-02]
must_haves:
  truths:
    - "Notes render Markdown correctly (e.g., # header becomes <h1>)"
    - "Clicking a note switches it to edit mode with a textarea"
    - "Blurring the textarea or clicking away saves the content and returns to preview mode"
    - "Rendered output is sanitized via DOMPurify"
  artifacts:
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Markdown rendering and inline editing logic"
  key_links:
    - from: "FlowEditorCanvas.vue"
      to: "marked"
      via: "import and parse"
    - from: "FlowEditorCanvas.vue"
      to: "dompurify"
      via: "import and sanitize"
---

<objective>
Implement the Markdown rendering engine and the inline editing toggle for canvas notes.
This allows users to write documentation using MD syntax and edit it directly without relying solely on the sidebar.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Markdown rendering logic</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Import `marked` and `DOMPurify` in the script section.
    - Create a helper function `renderMarkdown(content)` that:
      1. Uses `marked.parse(content)` to get HTML.
      2. Uses `DOMPurify.sanitize(html)` to clean it.
      3. Returns the sanitized HTML string.
    - Configure `marked` options if needed (e.g., `gfm: true`, `breaks: true`).
  </action>
  <verify>
    - Logic exists in script section.
    - No build errors when importing libraries.
  </verify>
  <done>Markdown rendering logic is available in the component.</done>
</task>

<task type="auto">
  <name>Task 2: Update Note template for Inline Editing</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    - Add a new ref `editingNoteId` (string or null).
    - Update the note rendering loop in the template:
      - If `editingNoteId === note.id`, render a `<textarea>` instead of the content div.
      - The `<textarea>` should be bound to `note.props.content` using `v-model`.
      - Add `@blur="editingNoteId = null"` to the textarea.
      - Ensure the textarea fills the note area (`width: 100%`, `height: 100%`, `resize: none`).
      - In "view mode" (when NOT editing), render the content using `v-html="renderMarkdown(note.props.content)"`.
    - Update `@click` on the note to set `editingNoteId.value = note.id`.
    - Add basic CSS for the textarea and markdown content (h1, p, ul) inside `.fec-note-content`.
  </action>
  <verify>
    - Clicking a note shows a textarea.
    - Typing Markdown and clicking outside shows the rendered HTML.
    - Check that # Headers and lists work.
  </verify>
  <done>Notes can be edited inline and display rendered Markdown.</done>
</task>

</tasks>

<verification>
- Create a note, type "# Hello", blur. Should see a large "Hello" header.
- Create a list, blur. Should see a bulleted list.
- Ensure script tags are stripped (sanitization check).
</verification>

<success_criteria>
- FR-04 is fully implemented.
- Markdown is rendered safely using TR-01 and TR-02 requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/27-markdown-engine-styling/27-01-SUMMARY.md`
</output>
