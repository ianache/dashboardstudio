---
phase: 36
plan: 1
type: feature
requirements: ["PROXY-02"]
depends_on: ["35"]
wave: 1
files_modified: ["bff/src/config.js", "bff/src/cubeToken.js", "bff/src/proxy.js", "bff/src/index.js"]
must_haves:
  truths:
    - "CubeJS queries work via /bff/cubejs with an authenticated session"
    - "CubeJS receives a valid HS256 JWT in the Authorization header"
  artifacts:
    - "bff/src/cubeToken.js exists and implements JWT signing"
  key_links:
    - from: "/bff/cubejs/*"
      to: "http://cubejs:4000/*"
      via: "http-proxy-middleware"
autonomous: true
---

# Plan: 36-01-PLAN - CubeJS Proxy & JWT Signing

Implement the CubeJS proxy in the BFF, including server-side JWT signing using the user's session context.

## Tasks

<task id="36-01-01">
  <name>Update BFF Configuration</name>
  <files>["bff/src/config.js"]</files>
  <action>Add cubejsUrl and verify cubejsSecret mapping to CUBEJS_API_SECRET expectations.</action>
  <verify>
    <automated>node -e "import config from './bff/src/config.js'; if(!config.cubejsUrl) throw new Error();"</automated>
  </verify>
  <done>Config updated with CubeJS URL and Secret.</done>
</task>

<task id="36-01-02">
  <name>Create Cube Token Utility</name>
  <files>["bff/src/cubeToken.js"]</files>
  <action>Implement signCubeToken(user) using jsonwebtoken to sign user context (sub, name, roles) with config.cubejsSecret.</action>
  <verify>
    <automated>node -e "import { signCubeToken } from './bff/src/cubeToken.js'; const t = signCubeToken({sub:'test'}); if(!t) throw new Error();"</automated>
  </verify>
  <done>Cube token utility implemented and verified.</done>
</task>

<task id="36-01-03">
  <name>Implement CubeJS Proxy</name>
  <files>["bff/src/proxy.js"]</files>
  <action>Create and export cubejsProxy with onProxyReq for token injection, onProxyRes for CORS header stripping, and ws: true for WebSocket support.</action>
  <verify>
    <automated>grep "ws: true" bff/src/proxy.js</automated>
  </verify>
  <done>CubeJS proxy implemented with token injection and WebSocket support.</done>
</task>

<task id="36-01-04">
  <name>Mount CubeJS Proxy</name>
  <files>["bff/src/index.js"]</files>
  <action>Mount cubejsProxy at /bff/cubejs protected by requireAuth and tokenRefresh.</action>
  <verify>
    <automated>grep "/bff/cubejs" bff/src/index.js</automated>
  </verify>
  <done>CubeJS proxy mounted and protected.</done>
</task>

## Verification

### Manual Verification
1. Call `GET /bff/cubejs/v1/load` from an authenticated session.
2. Verify CubeJS returns data.
3. Check CubeJS logs to confirm receipt of the token and validation of the security context.
