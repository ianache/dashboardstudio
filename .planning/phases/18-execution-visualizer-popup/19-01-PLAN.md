---
phase: 19-integration-data-wiring
plan: 19-01
type: execute
wave: 1
depends_on: ["18-01"]
files_modified: [
  "backend/app/api/endpoints/integration_flows.py",
  "dashboard-app/src/services/api.js",
  "dashboard-app/src/components/executions/FlowExecutionPopup.vue",
  "dashboard-app/src/views/IntegrationsView.vue"
]
autonomous: true
requirements: ["FR-01", "FR-05", "TR-02", "TR-04"]

must_haves:
  truths:
    - "User can see a 'search' icon in the Integrations table actions"
    - "Clicking the 'search' icon opens the FlowExecutionPopup"
    - "The popup shows the latest execution status and diagram path"
    - "The popup shows a warning if no executions exist for the flow"
  artifacts:
    - path: "dashboard-app/src/components/executions/FlowExecutionPopup.vue"
      provides: "Visualizer popup for a specific execution"
    - path: "dashboard-app/src/views/IntegrationsView.vue"
      provides: "Main view with the new action button and popup integration"
  key_links:
    - from: "IntegrationsView.vue"
      to: "FlowExecutionPopup.vue"
      via: "v-if and props (executionId, flowName, flowId)"
---

<objective>
Integrate the FlowExecutionPopup into the IntegrationsView table and card views, wiring it to automatically fetch and display the latest execution for any given flow.

Purpose: Allow users to quickly visualize the health and status of their integrations without leaving the main list.
Output: Enhanced IntegrationsView with a "Visualizar última ejecución" action.
</objective>

<execution_context>
@dashboard-app/src/views/IntegrationsView.vue
@dashboard-app/src/components/executions/FlowExecutionPopup.vue
@dashboard-app/src/services/api.js
@backend/app/api/endpoints/integration_flows.py
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From backend/app/api/endpoints/integration_flows.py:
- GET /api/v1/integration-flows/{flow_id}/executions (returns list of executions)
- GET /api/v1/integration-flows/executions/{exec_id}/logs (returns logs and node statuses)

From dashboard-app/src/services/api.js:
- integrationFlowsApi.getById(id) -> returns flow with {flow_nodes, flow_connections, flow_metadata}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data Wiring & API Service Enhancements</name>
  <files>backend/app/api/endpoints/integration_flows.py, dashboard-app/src/services/api.js, dashboard-app/src/components/executions/FlowExecutionPopup.vue</files>
  <action>
    1. Update backend `get_execution_logs` in `integration_flows.py` to include `flow_id` in the returned object (needed by the popup to fetch the diagram).
    2. Update `dashboard-app/src/services/api.js`:
        - Add `getLatestExecution(flowId)` using `/api/v1/integration-flows/${flowId}/executions?limit=1`.
        - Add `getExecutionLogs(execId)` using `/api/v1/integration-flows/executions/${execId}/logs`.
    3. Refactor `FlowExecutionPopup.vue`:
        - Use `integrationFlowsApi.getExecutionLogs` instead of direct `fetch`.
        - Add `flowId` as an optional prop.
        - Fix diagram data mapping: Use `{ nodes: flow.flow_nodes, connections: flow.flow_connections, metadata: flow.flow_metadata }` to correctly populate `FlowEditorCanvas`.
  </action>
  <verify>
    - Backend returns `flow_id` in execution logs endpoint.
    - `integrationFlowsApi` has new methods.
  </verify>
  <done>Backend and API services support the required data flow for the popup.</done>
</task>

<task type="auto">
  <name>Task 2: IntegrationsView UI Integration</name>
  <files>dashboard-app/src/views/IntegrationsView.vue</files>
  <action>
    1. Import `FlowExecutionPopup` in `IntegrationsView.vue`.
    2. Add reactive state for the popup: `showExecutionPopup`, `selectedExecutionId`, `selectedFlowId`, `selectedFlowName`.
    3. Implement `viewLatestExecution(flow)` logic:
        - Fetch latest execution using `integrationFlowsApi.getLatestExecution`.
        - If exists: set state and show popup.
        - If not: show a toast/alert saying "No executions found".
    4. Update Table View: Add a button with `search` icon (lupa) in the actions column.
    5. Update Card View: Add the same button in the card footer actions.
    6. Include the `<FlowExecutionPopup />` component in the template, passing required props and handling `@close`.
  </action>
  <verify>
    - Lupa icon appears in every row of the integrations table.
    - Clicking the lupa icon on a flow with executions opens the popup.
    - Clicking the lupa icon on a flow without executions shows a warning alert.
  </verify>
  <done>Popup is fully integrated into the Integrations list UI.</done>
</task>

<task type="auto">
  <name>Task 3: Responsiveness & End-to-End Verification</name>
  <files>dashboard-app/src/components/executions/FlowExecutionPopup.vue, dashboard-app/src/views/IntegrationsView.vue</files>
  <action>
    1. Adjust `FlowExecutionPopup.vue` styles for better responsiveness:
        - Reduce overlay padding on small screens (e.g., `padding: 10px` for max-width 768px).
        - Ensure header labels don't overlap.
    2. Verify path highlighting: Check that connections in the popup are green for successful nodes.
    3. Final check of the "Acciones" column alignment and button spacing in `IntegrationsView.vue`.
  </action>
  <verify>
    - Popup fits well on tablet/mobile screens.
    - Node badges and connection highlighting correctly reflect the latest execution data.
  </verify>
  <done>The visualizer integration is polished and responsive.</done>
</task>

</tasks>

<verification>
- Run a flow in the Editor.
- Go back to Integrations list.
- Click the "search" icon for that flow.
- Verify the popup shows exactly what happened (green path, node times).
</verification>

<success_criteria>
- [ ] Integrations table has a new "Visualizar" (lupa) action.
- [ ] Popup loads the latest execution data automatically.
- [ ] Diagram in popup correctly shows node statuses and highlighted paths.
- [ ] No regressions in existing integration management features.
</success_criteria>

<output>
After completion, create `.planning/phases/18-execution-visualizer-popup/19-01-SUMMARY.md`
</output>
