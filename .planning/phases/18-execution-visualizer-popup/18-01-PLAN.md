---
phase: 18-execution-visualizer-popup
plan: 18-01
type: execute
wave: 1
depends_on: []
files_modified: ["dashboard-app/src/components/editor/FlowEditorCanvas.vue", "dashboard-app/src/components/executions/FlowExecutionPopup.vue"]
autonomous: true
requirements: [FR-02, FR-03, FR-04, TR-01, TR-03]

must_haves:
  truths:
    - "FlowEditorCanvas can be rendered in read-only mode without toolbars or side panels."
    - "Nodes show a duration badge in the top-left when execution data is present."
    - "Nodes show a tooltip with start/end timestamps on hover."
    - "FlowExecutionPopup displays the flow diagram and execution status correctly."
  artifacts:
    - path: "dashboard-app/src/components/executions/FlowExecutionPopup.vue"
      provides: "Visual execution monitor modal"
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Read-only mode and execution badges"
  key_links:
    - from: "FlowExecutionPopup.vue"
      to: "FlowEditorCanvas.vue"
      via: "component inclusion with readOnly=true"
---

<objective>
Implement the frontend components for the Integration Flow Execution Visualizer, including a reusable popup and enhancements to the flow canvas for read-only visualization with execution metadata.
</objective>

<execution_context>
The project uses Vue 3 with Vite and Vanilla CSS. The `FlowEditorCanvas.vue` is the primary component for diagramming and needs to be adapted for visualization mode.
</execution_context>

<context>
@dashboard-app/src/components/editor/FlowEditorCanvas.vue
@dashboard-app/src/components/executions/ExecutionHistoryModal.vue
@.planning/ROADMAP.md

<interfaces>
From dashboard-app/src/components/editor/FlowEditorCanvas.vue (Existing Props):
```typescript
const props = defineProps({
  diagramType: { type: String, required: true },
  tools:       { type: Array,  default: () => [] },
  diagramData: { type: Object, default: () => ({ nodes: [], connections: [], metadata: {} }) },
  flowId:      { type: String, default: null }
})
```

From API (Execution Logs):
```typescript
interface NodeLog {
  node_id: string;
  status: 'running' | 'success' | 'error';
  start_time: string;
  end_time: string;
  duration: number; // ms or seconds
  input_data?: any;
  output_data?: any;
}

interface ExecutionDetail {
  id: string;
  status: string;
  node_logs: NodeLog[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt FlowEditorCanvas for Read-Only and Visualization</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
    1. Add `readOnly` (Boolean, default: false) and `executionData` (Object, default: null) to `defineProps`.
    2. In the template:
       - Wrap the left panel (`fec-left`), floating toolbar (`fec-toolbar`), and right panel inner content in `v-if="!readOnly"`.
       - If `readOnly` is true, ensure the `aside` for the right panel is either hidden or shows a minimized version.
       - Hide the bottom execution console if `readOnly`.
    3. In the interaction logic:
       - Update `onNodeMousedown`, `onPortMousedown`, `onCanvasMousedown`, `onCanvasMousewheel`, etc., to return early if `readOnly` is true (allow panning/zooming if desired, but disable dragging/creating).
    4. Implement Execution Badges in the node loop (`v-for="node in nodes"`):
       - Add `fec-node-badge--left` for duration (Top-left). Show if `executionData` has a log for the node.
       - Add `fec-node-tooltip` (using a simple `title` attribute or a floating div) showing "Inicio: {start_time}\nFin: {end_time}".
    5. Add CSS for:
       - `.fec-node-badge--left`: absolute, top: -10px, left: -10px, min-width: 22px, height: 22px, padding: 0 5px, background: #f1f5f9, color: #475569, border-radius: 11px, font-size: 10px, font-weight: 700, display: flex, align-items: center, justify-content: center, box-shadow: 0 2px 6px rgba(0,0,0,0.1), border: 1px solid #e2e8f0.
       - Ensure `fec-node-badge` (status) remains at top-right.
  </action>
  <verify>
    <automated>Search for "readOnly" and "badge--left" in the file to confirm implementation.</automated>
  </verify>
  <done>FlowEditorCanvas supports readOnly mode and displays execution durations and tooltips.</done>
</task>

<task type="auto">
  <name>Task 2: Create FlowExecutionPopup component</name>
  <files>dashboard-app/src/components/executions/FlowExecutionPopup.vue</files>
  <action>
    Create a new component `FlowExecutionPopup.vue`:
    1. Template:
       - Overlay/Backdrop with a centered modal container.
       - Header with Flow Name and Execution Status.
       - Main body containing `FlowEditorCanvas` in `readOnly` mode.
       - Loading state while fetching data.
    2. Script setup:
       - Props: `executionId` (required), `flowName` (optional).
       - State: `executionDetail`, `diagramData`, `loading`.
       - On mount: 
         - Fetch execution detail from `/api/v1/integration-flows/executions/{executionId}/logs`.
         - Fetch flow diagram (using the flow_id from the execution detail).
         - Populate `diagramData` and `executionData` for the canvas.
    3. Styling:
       - Modal should take ~90% of screen width/height for better diagram visibility.
       - Ensure z-index is high enough to cover other elements.
  </action>
  <verify>
    <automated>File exists and imports FlowEditorCanvas.</automated>
  </verify>
  <done>FlowExecutionPopup component created and wired to fetch execution data.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Visualizer Popup and Read-Only Canvas enhancements.</what-built>
  <how-to-verify>
    1. Manually mount `FlowExecutionPopup` in a temporary view or trigger it from `ExecutionHistoryModal.vue` with a hardcoded ID.
    2. Verify the diagram appears correctly.
    3. Confirm that no toolbars/side panels are visible.
    4. Verify the top-left duration badge appears on nodes.
    5. Verify the hover tooltip shows start/end times.
    6. Ensure connections are highlighted in green for success paths.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check that the popup works with static/mock data before moving to full integration in Phase 19.
</verification>

<success_criteria>
- FlowEditorCanvas renders correctly without editing controls when readOnly=true.
- Node durations are visible as badges.
- Tooltips display execution timestamps.
- FlowExecutionPopup displays the diagram with real execution data from the API.
</success_criteria>

<output>
After completion, create `.planning/phases/18-execution-visualizer-popup/18-01-SUMMARY.md`
</output>
