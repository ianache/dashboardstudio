---
phase: 41-pickle-model-node
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - dashboard-app/src/services/api.js
  - dashboard-app/src/views/SettingsView.vue
  - dashboard-app/src/components/editor/FlowEditorCanvas.vue
autonomous: true
requirements:
  - MODEL-01
  - MODEL-03

must_haves:
  truths:
    - "Users can upload and manage .pkl models from the Settings area."
    - "The Pickle Model node property panel shows a dropdown to select a model."
    - "The property panel displays the expected feature columns for the selected model as a guide."
  artifacts:
    - path: "dashboard-app/src/views/SettingsView.vue"
      provides: "Model management UI (list, upload, delete)"
      min_lines: 100
    - path: "dashboard-app/src/components/editor/FlowEditorCanvas.vue"
      provides: "Pickle Model node property panel with feature hints"
      min_lines: 50
---

<objective>
Implement the frontend UI for ML model management and flow node configuration.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-pickle-model-node/41-CONTEXT.md
@.planning/phases/41-pickle-model-node/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Service for ML Models</name>
  <files>dashboard-app/src/services/api.js</files>
  <action>
Add `mlModelsApi` to `dashboard-app/src/services/api.js`:
- `getAll()`: `GET /ml-models`
- `upload(file, name)`: `POST /ml-models` (multipart/form-data)
- `delete(id)`: `DELETE /ml-models/{id}`
  </action>
  <verify>
    <automated>grep "mlModelsApi" dashboard-app/src/services/api.js</automated>
  </verify>
  <done>Frontend API service is updated with ML model endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Model Management UI</name>
  <files>dashboard-app/src/views/SettingsView.vue</files>
  <action>
Modify `SettingsView.vue` (or add a tab/section):
1. Create a "Modelos ML" section.
2. List models with name, sklearn version, and creation date.
3. Add a "Subir Modelo (.pkl)" button with file selection.
4. Add a delete button for each model.
  </action>
  <verify>
    <automated>grep "Modelos ML" dashboard-app/src/views/SettingsView.vue</automated>
  </verify>
  <done>Users can now upload and delete .pkl models from the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Pickle Model Node Property Panel</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
Update `FlowEditorCanvas.vue`:
1. Ensure the `model_id` property of `pickle_model` nodes uses a `dynamic_select` (or equivalent dropdown).
2. Add a watcher or logic to fetch model details (features) when `model_id` is selected.
3. Render a "Columnas esperadas" (Expected Features) block in the property panel for `pickle_model` nodes.
  </action>
  <verify>
    <automated>grep "pickle_model" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>The Pickle Model node is fully configurable with feature metadata display.</done>
</task>

</tasks>

<verification>
1. **List Test**: Navigate to Settings -> verify models uploaded in Phase 41-01 appear in the list.
2. **Upload UI Test**: Upload a new model from the UI, verify it appears in the list and shows the correct version.
3. **Node Config Test**: Open a flow, add a Pickle Model node, select a model, and verify the list of features is displayed in the sidebar.
</verification>
