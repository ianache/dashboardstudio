---
phase: 41-pickle-model-node
plan: 03
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - backend/app/runtime/ml_worker.py
  - backend/app/services/ml_executor.py
  - backend/app/services/source_executor.py
  - backend/app/runtime/runner.ts
autonomous: true
requirements:
  - MODEL-02
  - MODEL-04

must_haves:
  truths:
    - "Inference runs in a separate subprocess, protecting the main backend from Pickle RCE."
    - "A version mismatch warning appears in the execution console if the model's sklearn version differs from the runner's."
  artifacts:
    - path: "backend/app/services/ml_executor.py"
      provides: "Python service to manage isolated inference calls"
    - path: "backend/app/runtime/runner.ts"
      provides: "Pre-executed warning propagation"
---

<objective>
Implement secure subprocess-isolated inference and integrate it into the flow execution engine.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-pickle-model-node/41-CONTEXT.md
@.planning/phases/41-pickle-model-node/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Isolated ML Worker (Predict Mode)</name>
  <files>backend/app/runtime/ml_worker.py</files>
  <action>
Update `backend/app/runtime/ml_worker.py` with `predict` logic:
1. For `predict`:
   - Read JSON payload from `sys.stdin`.
   - Convert payload to `pandas.DataFrame`.
   - Load model with `joblib.load(model_path)`.
   - Call `model.predict(df)`.
   - Convert results to list and print as JSON.
  </action>
  <verify>
    <automated>python backend/app/runtime/ml_worker.py --help</automated>
  </verify>
  <done>Worker script is fully functional for both metadata inspection and inference.</done>
</task>

<task type="auto">
  <name>Task 2: ML Executor Service with Version Check</name>
  <files>backend/app/services/ml_executor.py</files>
  <action>
Implement `backend/app/services/ml_executor.py`:
1. `execute_ml_node(props: dict, payload: any, db: Session)`:
   - Resolve `MLModel` from `model_id`.
   - Prepare `subprocess.Popen` for `ml_worker.py --mode predict`.
   - Write `payload` to worker's `stdin`.
   - Read result from worker's `stdout`.
   - Compare current `sklearn.__version__` with `model.sklearn_version`.
   - Return `{ "success": bool, "output": ..., "warning": str|None }`.
  </action>
  <verify>
    <automated>python -c "from app.services.ml_executor import execute_ml_node; print('Service OK')"</automated>
  </verify>
  <done>ML executor service handles isolated calls and version detection.</done>
</task>

<task type="auto">
  <name>Task 3: Integration & Runner Warning Propagation</name>
  <files>
    - backend/app/services/source_executor.py
    - backend/app/runtime/runner.ts
  </files>
  <action>
1. Update `source_executor.py`:
   - In `pre_execute_flow_nodes`, add a branch for `pickle_model`.
   - Call `execute_ml_node`.
   - On success: Store in `prefetched_outputs` including any `warning`.
2. Update `runner.ts`:
   - In the pre-execution result handling logic, check if `context.prefetched_outputs[node.id]` has a `warning`.
   - If a warning exists, print it as `[Model Warning] ...` so it appears in the execution console.
  </action>
  <verify>
    <automated>deno check backend/app/runtime/runner.ts</automated>
  </verify>
  <done>Pickle Model node is fully integrated with support for runtime warnings.</done>
</task>

</tasks>

<verification>
1. **End-to-End Inference**: Run a flow with a valid model and payload, verify the result in the console.
2. **Security Check**: Verify that `ml_worker.py` runs as a separate PID during execution.
3. **Version Mismatch**: Manually set a different version in the DB and verify the `[Model Warning]` appears in the console during run.
</verification>
