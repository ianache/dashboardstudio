---
phase: 29-metadata-inspection-api
plan: 29-01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/services/metadata_service.py]
autonomous: true
requirements: [TR-01]
must_haves:
  truths:
    - "MetadataService can fetch table names from a PostgreSQL database"
    - "MetadataService can fetch column names and types from a PostgreSQL table"
  artifacts:
    - path: "backend/app/services/metadata_service.py"
      provides: "Metadata inspection logic"
  key_links:
    - from: "backend/app/services/metadata_service.py"
      to: "asyncpg"
      via: "database connection"
---

<objective>
Implement the core service logic for database metadata inspection, focusing on PostgreSQL.

Purpose: Provide a clean, strategy-based interface for listing tables and columns in different database types.
Output: A new `metadata_service.py` file.
</objective>

<execution_context>
@backend/app/services/connection_testing.py
@backend/app/services/source_executor.py
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/app/services/connection_testing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetadataService structure</name>
  <files>backend/app/services/metadata_service.py</files>
  <action>
    Create a strategy-based service for metadata inspection.
    - Define `BaseMetadataStrategy` with `get_tables` and `get_columns` abstract methods.
    - Implement `PostgresMetadataStrategy` using `asyncpg`.
    - Implement `MetadataService` class to dispatch to the correct strategy based on connection type.
    - Handle `postgresql` and `postgres` types using the Postgres strategy.
    
    SQL for tables: `SELECT table_name FROM information_schema.tables WHERE table_schema = $1 AND table_type = 'BASE TABLE' ORDER BY table_name`
    SQL for columns: `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = $1 AND table_schema = $2 ORDER BY ordinal_position`
  </action>
  <verify>
    <automated>python -c "from app.services.metadata_service import metadata_service; print('MetadataService loaded')"</automated>
  </verify>
  <done>Service exists and can be imported without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Postgres inspection logic</name>
  <files>backend/app/services/metadata_service.py</files>
  <behavior>
    - `get_tables` should return a list of strings (table names).
    - `get_columns` should return a list of dicts with 'name' and 'type'.
    - Connection timeouts should be handled (e.g. 10s).
  </behavior>
  <action>
    Complete the implementation of `PostgresMetadataStrategy`.
    Ensure it uses `asyncpg.connect` with provided credentials (host, port, user, password, database).
    Always close the connection in a `finally` block.
  </action>
  <verify>
    <automated>python -c "from app.services.metadata_service import PostgresMetadataStrategy; import inspect; print(inspect.iscoroutinefunction(PostgresMetadataStrategy.get_tables))"</automated>
  </verify>
  <done>Implementation is complete and follows async patterns used in the project.</done>
</task>

</tasks>

<verification>
Check if the service can be imported and the methods are async.
</verification>

<success_criteria>
- `backend/app/services/metadata_service.py` exists.
- `MetadataService` supports PostgreSQL.
- Logic follows the user-provided SQL queries.
</success_criteria>

<output>
After completion, create `.planning/phases/29-metadata-inspection-api/29-01-SUMMARY.md`
</output>
