---
phase: 29-metadata-inspection-api
plan: 29-02
type: execute
wave: 2
depends_on: [29-01]
files_modified: [backend/app/api/endpoints/data_sources.py]
autonomous: true
requirements: [TR-01, FR-01, FR-04]
must_haves:
  truths:
    - "API exposes GET /api/v1/data-sources/{id}/tables"
    - "API exposes GET /api/v1/data-sources/{id}/tables/{table_name}/columns"
    - "API correctly decodes DataSource credentials before calling metadata_service"
  artifacts:
    - path: "backend/app/api/endpoints/data_sources.py"
      provides: "New inspection endpoints"
  key_links:
    - from: "backend/app/api/endpoints/data_sources.py"
      to: "backend/app/services/metadata_service.py"
      via: "service method call"
---

<objective>
Expose the metadata inspection functionality through FastAPI endpoints.

Purpose: Allow the frontend (and other services) to discover database structure dynamically.
Output: Updated `data_sources.py` with two new GET endpoints.
</objective>

<execution_context>
@backend/app/api/endpoints/data_sources.py
@backend/app/services/metadata_service.py
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/app/api/endpoints/data_sources.py
@backend/app/services/metadata_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inspection endpoints to data_sources.py</name>
  <files>backend/app/api/endpoints/data_sources.py</files>
  <action>
    Add two new endpoints to the `router` in `data_sources.py`:
    
    1. `GET /{id}/tables?schema=public`
       - Fetch DataSource by ID.
       - Decode config using `_decode_config`.
       - Inject `config["type"] = ds.type`.
       - Call `metadata_service.get_tables(config, schema)`.
       - Return list of strings.

    2. `GET /{id}/tables/{table_name}/columns?schema=public`
       - Fetch DataSource by ID.
       - Decode config using `_decode_config`.
       - Inject `config["type"] = ds.type`.
       - Call `metadata_service.get_columns(config, table_name, schema)`.
       - Return list of dicts: `[{"name": "...", "type": "..."}, ...]`.
    
    Ensure `metadata_service` is imported inside the functions or at module level (careful with circular imports if any, though service -> endpoint is usually fine).
  </action>
  <verify>
    <automated>python -c "from app.api.endpoints.data_sources import router; paths = [r.path for r in router.routes]; print('/{id}/tables' in paths and '/{id}/tables/{table_name}/columns' in paths)"</automated>
  </verify>
  <done>Endpoints are registered in the router.</done>
</task>

<task type="auto">
  <name>Task 2: Create verification script</name>
  <files>backend/test_metadata_api.py</files>
  <action>
    Create a script similar to `backend/test_api.py` to verify the new endpoints.
    It should:
    - Use `TestClient(app)`.
    - Mock `get_current_user`.
    - Find an existing PostgreSQL data source or create a dummy one for the test (if possible without a real DB, or just verify 404/500 if DB unreachable).
    - Since we don't have a live test DB easily accessible in this environment, focusing on the API routing and logic is key.
  </action>
  <verify>
    <automated>python backend/test_metadata_api.py</automated>
  </verify>
  <done>Verification script runs and confirms the endpoints are alive.</done>
</task>

</tasks>

<verification>
Run the verification script and check for successful routing.
</verification>

<success_criteria>
- Endpoints respond correctly at the specified paths.
- Proper error handling (404 if data source missing, 500 if connection fails).
</success_criteria>

<output>
After completion, create `.planning/phases/29-metadata-inspection-api/29-02-SUMMARY.md`
</output>
