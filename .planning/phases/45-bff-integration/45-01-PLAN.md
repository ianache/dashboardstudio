---
phase: 45-bff-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["bff/src/config.js", "bff/src/proxy.js", "bff/src/index.js", "bff/.env-bff.example", "environment/bi-backend.env"]
autonomous: true
requirements: ["SVC-04"]
must_haves:
  truths:
    - "GET /bff/ai/health returns 200 (proxied to ai-analyst)"
    - "POST /bff/ai/chat with valid session forwards to ai-analyst"
    - "X-User-ID header is present in proxied requests"
    - "SSE stream for /chat is not buffered"
  artifacts:
    - path: "bff/src/config.js"
      provides: "aiServiceUrl configuration"
    - path: "bff/src/proxy.js"
      provides: "aiProxy middleware"
    - path: "bff/src/index.js"
      provides: "Route mounting for /bff/ai"
  key_links:
    - from: "bff/src/index.js"
      to: "bff/src/proxy.js"
      via: "import { aiProxy }"
    - from: "bff/src/proxy.js"
      to: "process.env.BFF_AI_URL"
      via: "config.aiServiceUrl"
---

<objective>
Implement the BFF routes that proxy /bff/ai/* requests to the AI service with session validation and screen context passthrough.

Purpose: Secure access to the AI Analyst service from the frontend via the BFF.
Output: Updated BFF service with a new aiProxy, environmental configuration, and route mounting.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bff-integration/45-RESEARCH.md
@.planning/phases/45-bff-integration/45-CONTEXT.md
@bff/src/proxy.js
@bff/src/index.js
@bff/src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment & Configuration Setup</name>
  <files>bff/.env-bff.example, environment/bi-backend.env, bff/src/config.js</files>
  <action>
    1. Add `BFF_AI_URL=http://ai-analyst:8001` to `bff/.env-bff.example` and `environment/bi-backend.env`.
    2. Update `bff/src/config.js` to read `aiServiceUrl` from `process.env.BFF_AI_URL` (defaulting to `http://ai-analyst:8001`).
    3. Ensure the variable is exported in the `config` object.
  </action>
  <verify>
    <automated>grep "BFF_AI_URL" bff/.env-bff.example environment/bi-backend.env && grep "aiServiceUrl" bff/src/config.js</automated>
  </verify>
  <done>Environmental configuration is ready and loaded by the BFF.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Proxy in proxy.js</name>
  <files>bff/src/proxy.js</files>
  <action>
    1. Import `createProxyMiddleware` and `fixRequestBody` (already there).
    2. Export `aiProxy` using `createProxyMiddleware`.
    3. Configuration:
       - `target: config.aiServiceUrl`
       - `changeOrigin: true`
       - `proxyTimeout: 0` (Crucial for SSE)
       - `timeout: 0` (Crucial for SSE)
       - `pathRewrite: { '^/': '/' }` (The /bff/ai prefix is stripped by Express)
    4. Implement `on.proxyReq`:
       - Inject `X-User-ID` header from `req.session.user.sub` if present.
       - Inject `X-User-Email` header from `req.session.user.email` if present.
       - Call `fixRequestBody(proxyReq, req)` to handle body-parser stream issues.
    5. Implement `on.proxyRes`:
       - Strip upstream CORS headers (Access-Control-Allow-*) to ensure BFF remains the sole CORS owner.
    6. Implement `on.error`:
       - Log error and return 502 with JSON `{ error: 'AI service unreachable' }`.
  </action>
  <verify>
    <automated>node -e "import('./bff/src/proxy.js').then(m => console.log(m.aiProxy ? 'OK' : 'MISSING'))"</automated>
  </verify>
  <done>aiProxy is implemented with SSE support and user identity injection.</done>
</task>

<task type="auto">
  <name>Task 3: Mount Routes & Integration Check</name>
  <files>bff/src/index.js</files>
  <action>
    1. Import `aiProxy` from `./proxy.js`.
    2. Mount the route: `app.use('/bff/ai', requireAuth, tokenRefresh, aiProxy);`.
    3. Update the WebSocket upgrade handler to support `/bff/ai` (if applicable, though `/chat` is usually HTTP SSE). 
       *Note: Research indicates SSE, but adding WebSocket support for /bff/ai is consistent with existing patterns.*
  </action>
  <verify>
    <automated>grep "/bff/ai" bff/src/index.js && node -c bff/src/index.js</automated>
  </verify>
  <done>BFF routes for AI are mounted and protected by session authentication.</done>
</task>

</tasks>

<verification>
1. Verify `bff/src/config.js` contains `aiServiceUrl`.
2. Verify `bff/src/proxy.js` exports `aiProxy` with `timeout: 0` and `X-User-ID` injection.
3. Verify `bff/src/index.js` mounts `/bff/ai` with `requireAuth` and `tokenRefresh`.
4. (Optional Manual) Run `docker-compose up bff` and check if it starts without errors.
</verification>

<success_criteria>
- `/bff/ai/chat` is reachable via BFF with a valid session.
- Proxied requests include user identity headers.
- Long-lived SSE connections are supported without proxy-level timeouts.
</success_criteria>

<output>
After completion, create `.planning/phases/45-bff-integration/45-01-SUMMARY.md`
</output>
