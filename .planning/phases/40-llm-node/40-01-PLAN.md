---
phase: 40-llm-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-app/src/constants/connectionTypes.js
  - backend/alembic/versions/035_add_llm_tool.py
  - backend/app/services/llm_executor.py
  - backend/app/services/source_executor.py
  - dashboard-app/src/components/editor/FlowEditorCanvas.vue
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04

must_haves:
  truths:
    - "Users can create and encrypt LLM connections (DataSource) with Base URL and API Key."
    - "The LLM node supports separate System and User prompts with payload interpolation."
    - "LLM execution occurs in Python during pre-execution, ensuring API keys never reach the Deno process."
    - "429 Rate Limit errors are automatically retried in the backend."
    - "The UI shows a warning that LLM nodes cannot consume dynamic data from Data Transform nodes."
  artifacts:
    - path: "backend/app/services/llm_executor.py"
      provides: "Secure LLM execution logic with retries"
      min_lines: 60
    - path: "backend/alembic/versions/035_add_llm_tool.py"
      provides: "Alembic migration for llm tool"
      min_lines: 40
  key_links:
    - from: "source_executor.py"
      to: "llm_executor.py"
      via: "Function call in pre_execute_flow_nodes"
      pattern: "execute_llm_node"
---

<objective>
Implement the LLM node with secure Python-based pre-execution, automatic retries, and UI architectural warnings.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-llm-node/40-CONTEXT.md
@.planning/phases/40-llm-node/40-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register LLM Connection Type</name>
  <files>dashboard-app/src/constants/connectionTypes.js</files>
  <action>
Update `CONN_TYPES`, `CONN_DEFAULTS`, and `CONN_META` to include the `llm` type.

Settings:
- `value`: `llm`, `label`: `LLM (OpenAI Compatible)`
- Defaults: `{ url: 'https://api.openai.com/v1', api_key: '', model: 'gpt-4o' }`
- Meta: `icon: 'psychology'`, `fg: '#7c3aed'`, `bg: '#f5f3ff'`
  </action>
  <verify>
    <automated>grep "llm" dashboard-app/src/constants/connectionTypes.js</automated>
  </verify>
  <done>LLM connection type is selectable in the Integrations UI.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration — register llm tool</name>
  <files>backend/alembic/versions/035_add_llm_tool.py</files>
  <action>
Create migration `035` chained from `034`.

Tool details:
- `id`: `tool-llm-001`
- `type`: `llm`
- `name`: `LLM Completion`
- `description`: `Calls an OpenAI-compatible LLM endpoint securely from the backend`
- `icon`: `psychology`
- `category`: `source`
- `prop_defs`:
  ```json
  {
    "connection_id": { "label": "Connection", "type": "connection", "connection_type": "llm" },
    "system_prompt": { "label": "System Prompt", "type": "textarea", "placeholder": "You are a helpful assistant..." },
    "user_prompt": { "label": "User Prompt", "type": "textarea", "placeholder": "Summarize this: {{payload.text}}" },
    "temperature": { "label": "Temperature", "type": "number", "default": 0.7 },
    "max_tokens": { "label": "Max Tokens", "type": "number", "default": 1024 }
  }
  ```
- `default_props`: `{ "temperature": 0.7, "max_tokens": 1024 }`
- `applicable_diagram_types`: `["data-integration", "process-flow"]`

Run migration: `cd backend && uv run alembic upgrade head`
  </action>
  <verify>
    <automated>cd backend && uv run alembic current</automated>
  </verify>
  <done>LLM tool is registered in the database.</done>
</task>

<task type="auto">
  <name>Task 3: Backend — LLM Executor Service</name>
  <files>backend/app/services/llm_executor.py</files>
  <action>
Create `backend/app/services/llm_executor.py`.
Implement `execute_llm_node(props: dict, payload: dict)`:
1. Extract `url`, `api_key`, `model` from resolved props.
2. Render `user_prompt` using `jinja2.Template(user_prompt).render(payload=payload)`.
3. Construct OpenAI-compatible JSON payload.
4. Use `httpx.AsyncClient` to POST to `url + "/chat/completions"`.
5. **Implement Retry Logic**: Catch 429 status codes and retry up to 3 times with exponential backoff (1s, 2s, 4s) or using the `retry-after` header if present.
6. Return `{"success": True, "output": content}` or `{"success": False, "error": msg}`.
  </action>
  <verify>
    <automated>python -c "from app.services.llm_executor import execute_llm_node; print('Import OK')"</automated>
  </verify>
  <done>LLM execution service is ready for use.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate LLM in Pre-execution Loop & Scrub Credentials</name>
  <files>backend/app/services/source_executor.py</files>
  <action>
Modify `backend/app/services/source_executor.py`:
1. Import `execute_llm_node` from `app.services.llm_executor`.
2. Update the node loop in `pre_execute_flow_nodes`:
   - If `node.get("toolType") == "llm"`:
     - Execute the LLM call using `execute_llm_node`.
     - On success: Store result in `prefetched_outputs` and mark `__pre_executed = True`.
     - **CRITICAL SECURITY STEP**: Iterate through `node["props"]` and delete `api_key`, `password`, and `token` to ensure they never reach Deno.
     - On error: Set `pre_exec_ok = False` and break.
  </action>
  <verify>
    <automated>grep "execute_llm_node" backend/app/services/source_executor.py</automated>
  </verify>
  <done>LLM nodes are pre-executed and credentials are scrubbed before reaching Deno.</done>
</task>

<task type="auto">
  <name>Task 5: Frontend — LLM Architectural Warning</name>
  <files>dashboard-app/src/components/editor/FlowEditorCanvas.vue</files>
  <action>
Modify `FlowEditorCanvas.vue`:
1. Locate the property panel template where node properties are rendered.
2. Add a conditional warning block specifically for `llm` nodes.
3. Text: "⚠️ Los nodos LLM se ejecutan en el servidor por seguridad. No pueden consumir datos dinámicos generados por nodos 'Data Transform' o 'Templating' en el mismo flujo."
4. Add CSS class `fec-arch-warning` with yellow background and bold text.
  </action>
  <verify>
    <automated>grep "fec-arch-warning" dashboard-app/src/components/editor/FlowEditorCanvas.vue</automated>
  </verify>
  <done>User is warned about LLM input constraints in the editor.</done>
</task>

</tasks>

<verification>
1. **Security Test**: Debug the `flow_data` being sent to Deno and confirm `api_key` is MISSING for LLM nodes.
2. **Retry Test**: Mock a 429 response and verify the executor retries 3 times before failing.
3. **UI Test**: Select an LLM node and verify the "Architectural Warning" is visible in the sidebar.
4. **End-to-End**: Run a flow with `Initial Payload -> LLM Node -> Data Transform` and verify LLM output is correctly received by Deno.
</verification>
