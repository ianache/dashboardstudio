apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dashboardstudio.fullname" . }}-backend-configmap
  labels:
    {{- include "dashboardstudio.labels" . | nindent 4 }}
    component: backend
data:
  .env: |
    {{- range $key, $value := .Values.backend.env }}
    {{ $key }}={{ $value | quote }}
    {{- end }}
